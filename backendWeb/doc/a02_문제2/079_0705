1. JavaBeans는 어떤 목적으로 주로 사용되는가?
    1. 웹 프로그래밍에서 데이터의 표현
    2. 웹 프로그래밍에서 데이터의 처리
    3. 웹 프로그래밍에서 데이터의 저장
    4. 웹 프로그래밍에서 데이터의 전송

2. JavaBean의 프로퍼티 이름이 "propertyName"일 경우, getter 메서드의 이름은 무엇인가?
    1. public void propertyName(Type value)
    2. public Type getpropertyName()
    3. public Type getPropertyName() 대문자로 변경..
    4. public Type propertyName()

3. `<jsp:useBean>` 태그는 어떤 용도로 사용되는가?
    1. JSP에서 JavaBean 객체를 생성할 때
    2. JSP에서 JavaBean 객체를 삭제할 때
    3. JSP에서 JavaBean 객체를 수정할 때
    4. JSP에서 JavaBean 객체를 읽을 때

4. `<jsp:useBean>` 태그의 "scope" 속성은 무엇을 지정하는가?
    1. 자바빈 객체의 이름을 지정
    2. 자바빈 객체가 저장될 영역을 지정 : 언제까지 해당 객체를 가지고 있는가?
    3. 자바빈 클래스의 이름을 지정
    4. 자바빈 객체의 초기 값을 지정

5. `<jsp:setProperty>` 태그의 "property" 속성은 무엇을 지정하는가?
    1. 값을 설정할 프로퍼티의 이름을 지정
    2. 프로퍼티의 초기 값을 지정
    3. 프로퍼티의 데이터 유형을 지정
    4. 프로퍼티의 새 값을 지정

6. jQuery AJAX의 "url" 속성은 무엇을 지정하는가?
    1. 요청을 보낼 서버의 주소를 지정
    2. 요청의 데이터 유형을 지정
    3. 요청의 메서드 유형을 지정
    4. 요청의 상태 코드를 지정

7. jQuery AJAX의 "dataType" 속성은 무엇을 지정하는가?
    1. 요청의 데이터 유형을 지정
    2. 서버에서 받는 데이터의 유형을 지정
    3. 요청의 상태 코드를 지정
    4. 요청을 보낼 서버의 주소를 지정
    
    data :요청 query string
    dataType : 받는 데이터 유형 : success의 매개변수 data로
    	받는 데이터 유형에 영향을 미친다.
    success:function(data){
    }
    

8. jQuery AJAX의 "success" 속성은 무엇을 지정하는가?
    1. 요청이 성공했을 때 호출되는 함수를 지정
    2. 요청이 실패했을 때 호출되는 함수를 지정
    3. 요청을 보낼 서버의 주소를 지정
    4. 서버에서 받는 데이터의 유형을 지정

9. JavaBean의 getter 메서드에서 boolean 타입의 프로퍼티를 위해 사용할 수 있는 접두사는 무엇인가?
    1. set
    2. get
    3. is
    4. boolean

10. JavaBean의 setter 메서드에서 사용하는 접두사는 무엇인가?
    1. set
    2. get
    3. is
    4. boolean

11. `<jsp:useBean>` 태그의 "class" 속성은 무엇을 지정하는가?
    1. 자바빈 객체의 이름을 지정  id
    2. 자바빈 객체가 저장될 영역을 지정 scope
    3. 자바빈 클래스의 이름을 지정 class
    4. 자바빈 객체의 초기 값을 지정
    
    

12. `<jsp:setProperty>` 태그의 "value" 속성은 무엇을 지정하는가?
    1. 값을 설정할 프로퍼티의 이름을 지정
    2. 프로퍼티의 초기 값을 지정 :
    	 요청값을 받지 않고, 객체 생성 후 데이터 setXXX로 특정데이터 저장시
    3. 프로퍼티의 데이터 유형을 지정
    4. 프로퍼티의 새 값을 지정

13. jQuery AJAX의 "data" 속성은 무엇을 지정하는가?
    1. 요청을 보낼 서버의 주소를 지정
    2. 요청의 데이터 유형을 지정
    3. 요청에 포함될 데이터를 지정
    4. 서버에서 받는 데이터의 유형을 지정

14. jQuery AJAX의 "type" 속성은 무엇을 지정하는가?
    1. 요청을 보낼 서버의 주소를 지정
    2. 요청의 메서드 유형을 지정    get/post
    3. 요청에 포함될 데이터를 지정
    4. 서버에서 받는 데이터의 유형을 지정

15. JavaBean이 읽기 전용 프로퍼티를 가질 경우 어떤 메서드가 존재하는가?
    1. getter 메서드만 존재
    2. setter 메서드만 존재
    3. getter와 setter 메서드 둘 다 존재
    4. getter와 setter 메서드 둘 다 존재하지 않음
    
    
    
1. JavaBeans는 어떤 목적으로 주로 사용되는가?
    - 정답: 1. 웹 프로그래밍에서 데이터의 표현
    - JavaBeans는 데이터의 재사용 가능한 소프트웨어 컴포넌트를 생성하는데 사용됩니다. 이들은 주로 데이터를 표현하고 다루는 데 사용됩니다.

2. JavaBean의 프로퍼티 이름이 "propertyName"일 경우, getter 메서드의 이름은 무엇인가?
    - 정답: 3. public Type getPropertyName()
    - JavaBean의 프로퍼티에 접근하기 위해 사용되는 getter 메서드의 이름은 get + 프로퍼티 이름(첫 글자가 대문자)이 됩니다.

3. `<jsp:useBean>` 태그는 어떤 용도로 사용되는가?
    - 정답: 1. JSP에서 JavaBean 객체를 생성할 때
    - `<jsp:useBean>` 태그는 주로 JSP에서 JavaBean 객체를 생성하고, 이미 존재하면 해당 객체를 사용하는 데 사용됩니다.

4. `<jsp:useBean>` 태그의 "scope" 속성은 무엇을 지정하는가?
    - 정답: 2. 자바빈 객체가 저장될 영역을 지정
    - "scope" 속성은 자바빈 객체가 저장되는 영역(예: page, request, session, application)을 지정합니다.

5. `<jsp:setProperty>` 태그의 "property" 속성은 무엇을 지정하는가?
    - 정답: 1. 값을 설정할 프로퍼티의 이름을 지정
    - "property" 속성은 값을 설정할 프로퍼티의 이름을 지정합니다.

6. jQuery AJAX의 "url" 속성은 무엇을 지정하는가?
    - 정답: 1. 요청을 보낼 서버의 주소를 지정
    - "url" 속성은 HTTP 요청을 보낼 서버의 URL을 지정합니다.

7. jQuery AJAX의 "dataType" 속성은 무엇을 지정하는가?
    - 정답: 2. 서버에서 받는 데이터의 유형을 지정
    - "dataType" 속성은 서버에서 반환되는 데이터의 유형을 지정합니다.

8. jQuery AJAX의 "success" 속성은 무엇을 지정하는가?
    - 정답: 1. 요청이 성공했을 때 호출되는 함수를 지정
    - "success" 속성은 AJAX 요청이 성공했을 때 호출될 콜백 함수를 지정합니다.

9. JavaBean의 getter 메서드에서 boolean 타입의 프로퍼티를 위해 사용할 수 있는 접두사는 무엇인가?
    - 정답: 3. is
    - boolean 타입의 프로퍼티에 대한 getter 메서드는 'is'를 접두사로 사용합니다.

10. JavaBean의 setter 메서드에서 사용하는 접두사는 무엇인가?
    - 정답: 1. set
    - JavaBean의 모든 프로퍼티에 대한 setter 메서드는 'set'을 접두사로 사용합니다.

11. `<jsp:useBean>` 태그의 "class" 속성은 무엇을 지정하는가?
    - 정답: 3. 자바빈 클래스의 이름을 지정
    - "class" 속성은 사용하려는 JavaBean의 클래스 이름을 지정합니다.

12. `<jsp:setProperty>` 태그의 "value" 속성은 무엇을 지정하는가?
    - 정답: 4. 프로퍼티의 새 값을 지정
    - "value" 속성은 프로퍼티에 설정할 새 값을 지정합니다.

13. jQuery AJAX의 "data" 속성은 무엇을 지정하는가?
    - 정답: 3. 요청에 포함될 데이터를 지정
    - "data" 속성은 서버로 보내지는 데이터를 지정합니다.

14. jQuery AJAX의 "type" 속성은 무엇을 지정하는가?
    - 정답: 2. 요청의 메서드 유형을 지정
    - "type" 속성은 HTTP 요청 메서드(GET, POST 등)를 지정합니다.

15. JavaBean이 읽기 전용 프로퍼티를 가질 경우 어떤 메서드가 존재하는가?
    - 정답: 1. getter 메서드만 존재
    - 읽기 전용 프로퍼티는 getter 메서드만 존재하고, setter 메서드는 존재하지 않습니다. 이렇게 하면 프로퍼티의 값을 읽을 수는 있지만 변경할 수는 없습니다.
        
