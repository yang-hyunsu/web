1. 요청값은 주로 어떤 요소를 사용하여 서버로 전송되나요?
   a. form
   b. input
   c. button
   d. select

2. 다중데이터는 어떤 형태로 서버에서 전송된 데이터를 받나요?
   a. 객체 형태
   b. 문자열 형태
   c. 배열 형태
   d. 숫자 형태

3. form의 action 속성은 무엇을 지정하는 역할을 하나요?
   a. 서버 프로그램의 경로
   b. 데이터 전송 방식
   c. 입력 요소의 이름
   d. 데이터의 처리 방식

4. form 요소에서 사용자로부터 텍스트를 입력받는 형식은 무엇인가요?
   a. checkbox
   b. radio
   c. select
   d. text

5. 다음 중 체크박스에서 선택된 옵션만 서버로 전송하는 형식은 무엇인가요?
   a. text
   b. checkbox
   c. select
   d. radio

6. select 요소에서 여러 개의 옵션을 선택하기 위한 키보드 조합은 무엇인가요?
   a. Ctrl
   b. Shift
   c. Alt
   d. Tab

7. form 요소는 어떤 역할을 하는가?
a) 데이터베이스에 접속할 수 있는 권한을 부여한다.
b) 사용자가 서버에 데이터를 전송할 수 있도록 해주는 UI(user interface)이다.
c) 화면의 디자인과 레이아웃을 담당한다.
d) 서버에서 클라이언트로 데이터를 전송할 수 있도록 해주는 기능을 제공한다.

8. form 요소의 action 속성은 어떤 역할을 하는가?
a) form이 전송될 대상 페이지의 URL을 지정한다.
b) form의 디자인을 결정한다.
c) form 요소가 어떤 방식으로 전송될지를 결정한다.
d) form 요소의 값을 초기화한다.

9. form 요소의 name 속성과 value 속성은 각각 어떤 역할을 하는가?
a) name 속성은 form 요소의 식별자를 지정하고, value 속성은 초기값을 설정한다.
b) name 속성은 form 요소의 초기값을 설정하고, value 속성은 식별자를 지정한다.
c) name 속성은 form 요소의 값을 전송할 때 사용되는 이름을 지정하고, value 속성은 해당 값을 설정한다.
d) name 속성은 form 요소의 값을 초기화하고, value 속성은 값을 식별하는 데 사용된다.

10. form 요소를 통해 다중 데이터를 전송할 때 어떤 방식으로 처리할 수 있는가?
a) form 요소의 name 속성을 동일하게 설정하고, 서버에서 request.getParameterValues를 사용하여 배열로 처리할 수 있다.
b) form 요소의 name 속성을 모두 다르게 설정하고, 서버에서 request.getParameterValues를 사용하여 배열로 처리할 수 있다.
c) form 요소의 name 속성을 모두 다르게 설정하고, 서버에서 request.getParameter를 사용하여 개별적으로 처리할 수 있다.
d) form 요소의 name 속성을 동일하게 설정하고, 서버에서 request.getParameter를 사용하여 개별적으로 처리할 수 있다.

11. form 요소의 submit 버튼을 클릭하면 어떤 동작이 발생하는가?
a) form이서버로 전송되고, 서버에서 해당 페이지로 이동한다.
b) form이 서버로 전송되고, 서버에서 지정된 페이지로 이동한다.
c) form의 값을 초기화하고, 화면이 리로드된다.
d) form이 서버로 전송되고, 서버에서 지정된 페이지로 응답을 받아 처리한다.

12. radio 태그를 통해 선택한 옵션의 값은 어떻게 전송되는가?
a) 선택한 옵션의 value 속성값으로 전송된다.
b) 선택한 옵션의 텍스트로 전송된다.
c) 선택한 옵션의 인덱스로 전송된다.
d) 선택한 옵션의 name 속성값으로 전송된다.


1. 요청값은 주로 어떤 요소를 사용하여 서버로 전송되나요?
   a. form
   b. input
   c. button
   d. select

	a. form
	요청값은 주로 form 요소를 사용하여 서버로 전송된다. form 요소는 사용자가 입력한 데이터를 서버로 전송하기 위한 UI 요소이다.
	form 하위에 요소객체 name, value 속성을 이용하여
		key=value 형식으로 전송

2. 다중데이터는 어떤 형태로 서버에서 전송된 데이터를 받나요?
   a. 객체 형태
   b. 문자열 형태
   c. 배열 형태
   d. 숫자 형태

  c. 배열 형태
  다중 데이터는 form 요소의 name 속성을 동일하게 설정하고, 서버에서 request.getParameterValues를 사용하여 배열 형태로 받을 수 있다.
  String[] reqs = request.getParameterValues("key");

3. form의 action 속성은 무엇을 지정하는 역할을 하나요?
   a. 서버 프로그램의 경로
   b. 데이터 전송 방식
   c. 입력 요소의 이름
   d. 데이터의 처리 방식

   a. 서버 프로그램의 경로
   form의 action 속성은 form이 전송될 대상 페이지의 URL을 지정하는 역할을 한다. 즉, 서버 프로그램의 경로를 지정한다.
   <form action="rev.jsp"
   
   <form > 생략시, 현재 페이지에 요청값을 전달한다.
   ex) 현재 페이지 내용을 검색처리를 할 때, 주로 사용된다.
   
   

4. form 요소에서 사용자로부터 텍스트를 입력받는 형식은 무엇인가요?
   a. checkbox
   b. radio
   c. select
   d. text
   
   d. text
   form 요소에서 사용자로부터 텍스트를 입력받는 형식은 input 태그의 type 속성이 "text"인 요소이다

5. 다음 중 체크박스에서 선택된 옵션만 서버로 전송하는 형식은 무엇인가요?
   a. text
   b. checkbox
   c. select
   d. radio

   b. checkbox
   체크박스는 사용자가 선택한 옵션만 서버로 전송되는 형식이다. 선택한 체크박스에 대한 값만 전송된다.

6. select 요소에서 여러 개의 옵션을 선택하기 위한 키보드 조합은 무엇인가요?
   a. Ctrl
   b. Shift
   c. Alt
   d. Tab

   a. Ctrl
   select 요소에서 여러 개의 옵션을 선택하기 위해서는 Ctrl 키를 사용한다. Ctrl 키를 누른 상태에서 옵션을 클릭하면 여러 개의 옵션을 선택할 수 있다.


# 7. form 요소는 어떤 역할을 하는가?
a) 데이터베이스에 접속할 수 있는 권한을 부여한다.
b) 사용자가 서버에 데이터를 전송할 수 있도록 해주는 UI(user interface)이다.
c) 화면의 디자인과 레이아웃을 담당한다.
d) 서버에서 클라이언트로 데이터를 전송할 수 있도록 해주는 기능을 제공한다.

b. 사용자가 서버에 데이터를 전송할 수 있도록 해주는 UI(user interface)이다.
결국 query string을 사용자가 편한게 입력해서 전송하기 위한 역할을
form요소가 처리한다.
 

8. form 요소의 action 속성은 어떤 역할을 하는가?
a) form이 전송될 대상 페이지의 URL을 지정한다.
b) form의 디자인을 결정한다.
c) form 요소가 어떤 방식으로 전송될지를 결정한다.
d) form 요소의 값을 초기화한다.

a. form이 전송될 대상 페이지의 URL을 지정한다.


9. form 요소의 name 속성과 value 속성은 각각 어떤 역할을 하는가?
a) name 속성은 form 요소의 식별자를 지정하고, value 속성은 초기값을 설정한다.
b) name 속성은 form 요소의 초기값을 설정하고, value 속성은 식별자를 지정한다.
c) name 속성은 form 요소의 값을 전송할 때 사용되는 이름을 지정하고, value 속성은 해당 값을 설정한다.
d) name 속성은 form 요소의 값을 초기화하고, value 속성은 값을 식별하는 데 사용된다.

c. name 속성은 form 요소의 값을 전송할 때 사용되는 이름을 지정하고, value 속성은 해당 값을 설정한다.
==> query string 페이지?name속성값=value속성값&....

10. form 요소를 통해 다중 데이터를 전송할 때 어떤 방식으로 처리할 수 있는가?
a) form 요소의 name 속성을 동일하게 설정하고, 서버에서 request.getParameterValues를 사용하여 배열로 처리할 수 있다.
b) form 요소의 name 속성을 모두 다르게 설정하고, 서버에서 request.getParameterValues를 사용하여 배열로 처리할 수 있다.
c) form 요소의 name 속성을 모두 다르게 설정하고, 서버에서 request.getParameter를 사용하여 개별적으로 처리할 수 있다.
d) form 요소의 name 속성을 동일하게 설정하고, 서버에서 request.getParameter를 사용하여 개별적으로 처리할 수 있다.

a. form 요소의 name 속성을 동일하게 설정하고, 서버에서 request.getParameterValues를 사용하여 배열로 처리할 수 있다.


11. form 요소의 submit 버튼을 클릭하면 어떤 동작이 발생하는가?
a) form이 서버로 전송되고, 서버에서 해당 페이지로 이동한다.
b) form이 서버로 전송되고, 서버에서 지정된 페이지로 이동한다.
c) form의 값을 초기화하고, 화면이 리로드된다.
d) form이 서버로 전송되고, 서버에서 지정된 페이지로 응답을 받아 처리한다.

b. form이 서버로 전송되고, 서버에서 지정된 페이지로 이동한다.
document.querySelector("form").submit();

12. radio 태그를 통해 선택한 옵션의 값은 어떻게 전송되는가?
a) 선택한 옵션의 value 속성값으로 전송된다.
b) 선택한 옵션의 텍스트로 전송된다.
c) 선택한 옵션의 인덱스로 전송된다.
d) 선택한 옵션의 name 속성값으로 전송된다.
선택한 단일 데이터만 key=val로 전달..
a. 선택한 옵션의 value 속성값으로 전송된다.

