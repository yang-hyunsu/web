1. 데이터베이스에서 데이터 무결성 제약 조건에 대한 설명으로 옳은 것은?
   a) 데이터의 정확성을 유지하기 위해 사용한다.
   b) 테이블 생성 후 제약 조건을 변경할 수 없다.
   c) 제약 조건은 유연하게 활성화 및 비활성화할 수 있다.
   d) 제약 조건은 데이터 딕셔너리에 저장되어 응용 프로그램에서 동일하게 적용된다.

2. 데이터베이스에서 not null 제약 조건에 대한 설명으로 옳은 것은?
   a) 특정 컬럼에 null 값을 포함할 수 없다.
   b) 컬럼 값은 반드시 존재해야 하며 유일해야 한다.
   c) 컬럼의 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.
   d) 컬럼의 모든 행에서 고유한 값을 가져야 한다.

3. 데이터베이스에서 unique key 제약 조건에 대한 설명으로 옳은 것은?
   a) 모든 행에서 고유한 값을 가져야 한다.
   b) 컬럼에 null 값을 입력할 수 없다.
   c) 다양한 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.
   d) 다른 테이블의 컬럼과의 외래 키 관계를 설정하고 시행한다.

4. 데이터베이스에서 primary key 제약 조건에 대한 설명으로 옳은 것은?
   a) 컬럼에 null 값을 입력할 수 없다.
   b) 컬럼 값은 반드시 존재해야 하며 유일해야 한다.
   c) 모든 행에서 고유한 값을 가져야 한다.
   d) 컬럼의 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.

5. 데이터베이스에서 foreign key 제약 조건에 대한 설명으로 옳은 것은?
   a) 다른 테이블의 컬럼과의 외래 키 관계를 설정하고 시행한다.
   b) 컬럼에 null 값을 입력할 수 없다.
   c) 컬럼 값은 반드시 존재해야 하며 유일해야 한다.
   d) 다양한 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.

6. 데이터베이스에서 check 제약 조건에 대한 설명으로 옳은 것은?
   a) 컬럼에 null 값을 입력할 수 없다.
   b) 컬럼 값은 반드시 존재해야 하며 유일해야 한다.
   c) 컬럼의 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.
   d) 모든 행에서 고유한 값을 가져야 한다

.

7. 데이터베이스에서 sequence 객체에 대한 설명으로 옳은 것은?
   a) 유일한 식별자의 데이터를 자동으로 생성하기 위해 사용한다.
   b) 테이블 생성 후에만 sequence를 정의할 수 있다.
   c) sequence 객체는 데이터베이스의 메모리 캐시에 저장된다.
   d) sequence 객체는 현재 번호를 가져올 때 사용한다.

8. 데이터베이스에서 primary key의 데이터 입력에 사용하는 sequence 객체의 형식은?
   a) create sequence 시퀀스명 increment by 증가단위
   b) create sequence 시퀀스명 start with 시작번호
   c) create sequence 시퀀스명 minvalue 최소값
   d) create sequence 시퀀스명 maxvalue 최대값

9. 데이터베이스에서 외래 키 관계를 설정하기 위한 제약 조건은?
   a) unique key
   b) not null
   c) check
   d) foreign key

10. 데이터베이스에서 데이터 무결성 제약 조건의 역할은 무엇인가?
   a) 데이터의 정확성과 일관성을 보장한다.
   b) 테이블 생성 후 제약 조건을 정의할 수 없다.
   c) 제약 조건은 테이블마다 독립적으로 적용된다.
   d) 제약 조건은 데이터 입력 시에만 적용된다.
   
   
 1. 데이터베이스에서 데이터 무결성 제약 조건에 대한 설명으로 옳은 것은?
   a) 데이터의 정확성을 유지하기 위해 사용한다.
   b) 테이블 생성 후 제약 조건을 변경할 수 없다.
   c) 제약 조건은 유연하게 활성화 및 비활성화할 수 있다.
   d) 제약 조건은 데이터 딕셔너리에 저장되어 응용 프로그램에서 동일하게 적용된다.
	정답: a,c,d) 데이터의 정확성을 유지하기 위해 사용한다.
	          제약 조건은 유연하게 활성화 및 비활성화할 수 있다.
	          제약 조건은 데이터 딕셔너리에 저장되어 응용 프로그램에서 동일하게 적용된다.
	데이터 딕셔너리 : 데이터베이스의 데이터베이스인 메타데이터 베이스로
	데이터베이스의 객체(테이블, sequence, view등)을 정보를 가지고
	확인할 수 있다.          
	          
	데이터의 정확성을 유지하기 위해 사용한다.
	해설: 데이터 무결성 제약 조건은 데이터의 정확성과 일관성을 보장하기 위해 사용됩니다. 
	제약 조건은 특정 컬럼 또는 테이블에 적용되어 특정 규칙을 준수하도록 강제합니다.

2. 데이터베이스에서 not null 제약 조건에 대한 설명으로 옳은 것은?
   a) 특정 컬럼에 null 값을 포함할 수 없다.
   b) 컬럼 값은 반드시 존재해야 하며 유일해야 한다.
   c) 컬럼의 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.
   d) 컬럼의 모든 행에서 고유한 값을 가져야 한다.

정답: a) 특정 컬럼에 null 값을 포함할 수 없다.
해설: not null 제약 조건은 특정 컬럼에 null 값을 포함할 수 없도록 제한합니다. 즉, 해당 컬럼은 반드시 값이 존재해야 합니다.

3. 데이터베이스에서 unique key 제약 조건에 대한 설명으로 옳은 것은?
   a) 모든 행에서 고유한 값을 가져야 한다.
   b) 컬럼에 null 값을 입력할 수 없다.
   c) 다양한 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.
   d) 다른 테이블의 컬럼과의 외래 키 관계를 설정하고 시행한다.

정답: a) 모든 행에서 고유한 값을 가져야 한다.
해설: unique key 제약 조건은 해당 컬럼 또는 컬럼 조합에서 모든 행에서 고유한 값을 가져야 한다는 제약을 설정합니다.

4. 데이터베이스에서 primary key 제약 조건에 대한 설명으로 옳은 것은?
   a) 컬럼에 null 값을 입력할 수 없다.
   b) 컬럼 값은 반드시 존재해야 하며 유일해야 한다.
   c) 모든 행에서 고유한 값을 가져야 한다.
   d) 컬럼의 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.

정답: c) 모든 행에서 고유한 값을 가져야 한다.
		not null + unique
		not null : null을 넣지 않아야 된다.
		unique : 유일 데이터(null 입력 및 중복 허용)
			1
			2
			3
			null
			null
		고유값이라는 범위가 null값도 중복되지 않고, 유일값이라는
		의미에서 c로 정답을 본다.	

해설: primary key 제약 조건은 해당 컬럼 또는 컬럼 조합에서 모든 행에서 고유한 값을 가져야 하며, null 값을 포함할 수 없습니다.
     a) 부분적으로 맞긴한다.

5. 데이터베이스에서 foreign key 제약 조건에 대한 설명으로 옳은 것은?
   a) 다른 테이블의 컬럼과의 외래 키 관계를 설정하고 시행한다.
   b) 컬럼에 null 값을 입력할 수 없다.
   c) 컬럼 값은 반드시 존재해야 하며 유일해야 한다.
   d) 다양한 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.

정답: a) 다른 테이블의 컬럼과의 외래 키 관계를 설정하고 시행한다.
해설: foreign key 제약 조건은 한 테이블의 컬럼이 다른 테이블의 컬럼과 외래 키 관계를 맺고, 해당 관계를 시행하는 제약 조건입니다.

6. 데이터베이스에서 check 제약 조건에 대한 설명으로 옳은 것은?
   a) 컬럼에 null 값을 입력할 수 없다.
   b) 컬럼 값은 반드시 존재해야 하며 유일해야 한다.
   c) 컬럼의 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.
   d) 모든 행에서 고유한 값을 가져야 한다

정답: c) 컬럼의 데이터 범위나 조건을 지정하여 저장 가능한 값을 제한한다.
해설: check 제약 조건은 특정 컬럼에 대해 데이터의 범위나 조건을 지정하여 저장 가능한 값을 제한하는 제약 조건입니다.


7. 데이터베이스에서 sequence 객체에 대한 설명으로 옳은 것은?
   a) 유일한 식별자의 데이터를 자동으로 생성하기 위해 사용한다.
   b) 테이블 생성 후에만 sequence를 정의할 수 있다.
   		생성전에 따라 만들어 사용될 수 있다.
   c) sequence 객체는 데이터베이스의 메모리 캐시에 저장된다.
   d) sequence 객체는 현재 번호를 가져올 때 사용한다.

	sequence 객체는 데이터베이스의 메모리 캐시에 저장된다.
	==> 하드웨어적으로 특정한 메모리 공간에 저장한다.
		cache 옵션에 의해서 캐시메모리를 사용할 수 있다.
정답: a) 유일한 식별자의 데이터를 자동으로 생성하기 위해 사용한다.
     d) sequence 객체는 현재 번호를 가져올 때 사용한다.
     	seq01.nextval (증가되면서 다음 번호 가져오기)
     	seq01.currval (현재 번호 가져오기..)
해설: sequence 객체는 데이터베이스에서 유일한 식별자 값을 자동으로 생성하기 위해 사용되는 객체입니다.


8. 데이터베이스에서 primary key의 데이터 입력에 사용하는 sequence 객체의 형식은?
   a) create sequence 시퀀스명 increment by 증가단위
   b) create sequence 시퀀스명 start with 시작번호
   c) create sequence 시퀀스명 minvalue 최소값
   d) create sequence 시퀀스명 maxvalue 최대값

정답: a,b,c,d) create sequence 시퀀스명 start with 시작번호
	create sequence 시퀀스명;로 특정 버젼부터 기본 속성 없이도
	지원하게 됨. 

해설: primary key의 데이터 입력에 사용하는 sequence 객체를 생성할 때는 시작 번호를 지정하는 start with 절을 사용합니다.
데이터 입력에 사용 ==> 
	시퀀스 데이터 생성에 필요한 형식인지 : 반드시 필요한 형식 아님
		지정된 데이터로 입력해도 되기에...
		member: id, pass
		id : primary key 시퀀스를 사용할 필요 없음.
	사용하는 형식인지
	    numbering을 이용해서 key를 사용하거나,
	    	numbering과 문자열이 조합된 형식일 때 사용된다.
	    	


9. 데이터베이스에서 외래 키 관계를 설정하기 위한 제약 조건은?
   a) unique key
   b) not null
   c) check
   d) foreign key

정답: d) foreign key
해설: 외래 키 관계를 설정하기 위해 사용하는 제약 조건은 foreign key입니다.

10. 데이터베이스에서 데이터 무결성 제약 조건의 역할은 무엇인가?
   a) 데이터의 정확성과 일관성을 보장한다.
   b) 테이블 생성 후 제약 조건을 정의할 수 없다.
   c) 제약 조건은 테이블마다 독립적으로 적용된다.
   d) 제약 조건은 데이터 입력 시에만 적용된다.

정답: a) 데이터의 정확성과 일관성을 보장한다.
해설: 데이터 무결성 제약 조건은 데이터의 정확성과 일관성을 보장하기 위해 사용됩니다. 이를 통해 데이터베이스 내의 데이터가 유효하고 정확한 상태를 유지할 수 있습니다.
    제약 조건은 데이터 입력 시에만 적용된다. 
    	입력뿐만 아니라, 수정시 사용도 사용 가능...
    	delete시도 foreign key로 연결되어 있으면 에러가 나온다.
    	ex) 부서테이블(부서번호) ==> 사원(부서번호) 10
    	    사원 정보에 등록된 부서번호가 10이 있을 때,
    	    부서테이블에 10 행을 삭제시 에러 발생..
10:10~   	    
    	     

     
   