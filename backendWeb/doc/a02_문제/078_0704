1. JSTL은 무엇의 약자인가요?
   a) Java Server Technology Library
   b) Java Standard Tag Library
   c) Java Server Tag Library
   d) Java Standard Technology Library

2. JSTL을 사용하기 위해 필요한 라이브러리 파일은 무엇인가요?
   a) jstl-core.jar
   b) jstl.jar
   c) jstl-api.jar
   d) jstl-1.2.jar

3. JSTL에서 변수와 객체를 설정하고 변경하거나 삭제하기 위해 사용하는 태그는 무엇인가요?
   a) \<c:set>
   b) \<c:if>
   c) \<c:forEach>
   d) \<c:choose>

4. JSTL을 사용하여 조건문을 처리하기 위해 사용하는 태그는 무엇인가요?
   a) \<c:set>
   b) \<c:if>
   c) \<c:forEach>
   d) \<c:choose>

5. JSTL을 사용하여 반복문을 처리하기 위해 사용하는 태그는 무엇인가요?
   a) \<c:set>
   b) \<c:if>
   c) \<c:forEach>
   d) \<c:choose>

6. JSTL에서 변수를 선언하고 값을 할당하는 방법은 다음 중 어떤 것인가요?
   a) \<c:set var="변수명" value="값" scope="session"/>
   b) \<c:var name="변수명" value="값" scope="session"/>
   c) \<c:set name="변수명" value="값" scope="session"/>
   d) \<c:variable name="변수명" value="값" scope="session"/>


7. JSTL에서 조건문의 그 외 조건을 처리하기 위해 사용하는 태그는 무엇인가요?
   a) \<c:if>
   b) \<c:when>
   c) \<c:otherwise>
   d) \<c:choose>

8. JSTL에서 반복문을 처리할 때 시작값을 설정하는 속성은 무엇인가요?
    a) begin
    b) start
    c) from
    d) initial

9. JSTL에서 반복문을 처리할 때 마지막값을 설정하는 속성은 무엇인가요?
    a) end
    b) last
    c) to
    d) final

10. JSTL에서 반복문을 처리할 때 증감값을 설정하는 속성은 무엇인가요?
    a) step
    b) increase
    c) increment
    d) add

11. JSTL에서 반복문을 처리할 때 현재 실행 중인 루프의 첫 번째 실행인지 확인하는 변수는 무엇인가요?
    a) first
    b) start
    c) initial
    d) beginning

12. JSTL에서 반복문을 처리할 때 현재 실행 중인 루프의 마지막 실행인지 확인하는 변수는 무엇인가요?
    a) last
    b) end
    c) final
    d) finish



1. JSTL은 무엇의 약자인가요?
   a) jsp Server Technology Library
   b) jsp Standard Tag Library
   c) jsp Server Tag Library
   d) jsp Standard Technology Library

b) jsp Standard Tag Library

JSTL은 jsp Standard Tag Library의 약자입니다. JSP 페이지에서 자주 사용되는 태그들의 집합이며, JSP 개발을 편리하게 도와줍니다.


2. JSTL을 사용하기 위해 필요한 라이브러리 파일은 무엇인가요?
   a) jstl-core.jar
   b) jstl.jar
   c) jstl-api.jar
   d) jstl-1.2.jar

d) jstl-1.2.jar
JSTL을 사용하기 위해 필요한 라이브러리 파일은 "jstl-1.2.jar"입니다. 
	해당 라이브러리 파일은 JSTL의 핵심 기능을 제공합니다.


3. JSTL에서 변수와 객체를 설정하고 변경하거나 삭제하기 위해 사용하는 태그는 무엇인가요?
   a) \<c:set>
   b) \<c:if>
   c) \<c:forEach>
   d) \<c:choose>

a) <c:set> : 설정 변수, 변경
변수와 객체를 설정하고 변경하거나 삭제하기 위해 <c:set> 태그를 사용합니다. 이 태그를 사용하여 변수를 선언하고 값을 할당할 수 있습니다.
   <c:remove>

4. JSTL을 사용하여 조건문을 처리하기 위해 사용하는 태그는 무엇인가요?
   a) \<c:set>
   b) \<c:if>
   c) \<c:forEach>
   d) \<c:choose>

b) <c:if test="${조건문}">
JSTL에서 조건문을 처리하기 위해 <c:if> 태그를 사용합니다. <c:if> 태그는 조건식을 평가하고, 조건에 따라 내부의 코드 블록을 실행할지 결정합니다.


5. JSTL을 사용하여 반복문을 처리하기 위해 사용하는 태그는 무엇인가요?
   a) \<c:set>
   b) \<c:if>
   c) \<c:forEach>
   d) \<c:choose>

c) <c:forEach>
JSTL에서 반복문을 처리하기 위해 <c:forEach> 태그를 사용합니다. 이 태그를 사용하여 컬렉션의 각 요소에 대해 반복 작업을 수행할 수 있습니다.


6. JSTL에서 변수를 선언하고 값을 할당하는 방법은 다음 중 어떤 것인가요?
   a) \<c:set var="변수명" value="값" scope="session"/>
   b) \<c:var name="변수명" value="값" scope="session"/>
   c) \<c:set name="변수명" value="값" scope="session"/>
   d) \<c:variable name="변수명" value="값" scope="session"/>

a) <c:set var="변수명" value="값" scope="session"/>
JSTL에서 변수를 선언하고 값을 할당하는 방법은 <c:set> 태그를 사용하는 것입니다. <c:set> 태그의 var 속성으로 변수명을 지정하고, value 속성으로 값을 할당합니다.

7. JSTL에서 조건문의 그 외 조건을 처리하기 위해 사용하는 태그는 무엇인가요?
   a) \<c:if>
   b) \<c:when>
   c) \<c:otherwise>
   d) \<c:choose>

c) <c:otherwise>
JSTL에서 조건문의 그 외 조건을 처리하기 위해 <c:otherwise> 태그를 사용합니다. <c:choose> 태그와 함께 사용하여 여러 개의 조건을 처리할 수 있습니다.

8. JSTL에서 반복문을 처리할 때 시작값을 설정하는 속성은 무엇인가요?
    a) begin
    b) start
    c) from
    d) initial

a) begin
JSTL에서 반복문을 처리할 때 시작값을 설정하는 속성은 "begin"입니다. <c:forEach> 태그의 begin 속성에 시작값을 지정하여 반복문을 시작할 수 있습니다.

9. JSTL에서 반복문을 처리할 때 마지막값을 설정하는 속성은 무엇인가요?
    a) end
    b) last
    c) to
    d) final

a) end
JSTL에서 반복문을 처리할 때 마지막값을 설정하는 속성은 "end"입니다. <c:forEach> 태그의 end 속성에 마지막값을 지정하여 반복문을 제한할 수 있습니다.


10. JSTL에서 반복문을 처리할 때 증감값을 설정하는 속성은 무엇인가요?
    a) step
    b) increase
    c) increment
    d) add

a) step
JSTL에서 반복문을 처리할 때 증감값을 설정하는 속성은 "step"입니다. <c:forEach> 태그의 step 속성에 증감값을 지정하여 반복문의 증감 단위를 조절할 수 있습니다.


11. JSTL에서 반복문을 처리할 때 현재 실행 중인 루프의 첫 번째 실행인지 확인하는 변수는 무엇인가요?
    a) first
    b) start
    c) initial
    d) beginning

a) first
JSTL에서 반복문을 처리할 때 현재 실행 중인 루프의 첫 번째 실행인지 확인하는 변수는 "first"입니다. <c:forEach> 태그 내부에서 사용할 수 있으며, true/false 값을 가집니다

<c:if test="${sts.first}"> 첫번째 데이터일때 처리.
<c:if test="${sts.last}"> 마지막 데이터일때 처리.



12. JSTL에서 반복문을 처리할 때 현재 실행 중인 루프의 마지막 실행인지 확인하는 변수는 무엇인가요?
    a) last
    b) end
    c) final
    d) finish

a) last
JSTL에서 반복문을 처리할 때 현재 실행 중인 루프의 마지막 실행인지 확인하는 변수는 "last"입니다. <c:forEach> 태그 내부에서 사용할 수 있으며, true/false 값을 가집니다.

10:05~


