1. 정규화의 목적은 무엇인가요?
   a) 데이터 중복을 최소화하고 데이터 일관성을 유지하기 위함입니다.
   b) 데이터를 효율적으로 저장하기 위함입니다.
   c) 데이터의 보안을 강화하기 위함입니다.
   d) 데이터를 정렬하고 검색하기 위함입니다.

2. 이상 현상 중에서 삽입 이상에 대한 설명으로 옳은 것은?
   a) 중복 튜플 중 일부만 변경하여 데이터가 불일치하게 되는 문제입니다.
   b) 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입해야 하는 문제입니다.
   c) 튜플을 삭제하면 꼭 필요한 데이터까지 삭제되는 데이터 손실의 문제입니다.
   d) 테이블 간의 연관관계가 존재하지 않아 데이터의 일관성이 유지되지 않는 문제입니다.

3. 정규화 과정은 무엇을 의미하나요?
   a) 데이터의 중복을 최소화하면서 데이터베이스를 무결성있게 설계하는 과정입니다.
   b) 데이터를 효율적으로 저장하는 과정입니다.
   c) 데이터의 보안을 강화하는 과정입니다.
   d) 데이터를 정렬하고 검색하는 과정입니다.

4. 이상 현상 중에서 갱신 이상에 대한 설명으로 옳은 것은?
   a) 중복 튜플 중 일부만 변경하여 데이터가 불일치하게 되는 문제입니다.
   b) 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입해야 하는 문제입니다.
   c) 튜플을 삭제하면 꼭 필요한 데이터까지 삭제되는 데이터 손실의 문제입니다.
   d) 테이블 간의 연관관계가 존재하지 않아 데이터의 일관성이 유지되지 않는 문제입니다.

5. 이상 현상 중에서 삭제 이상에 대한 설명으로 옳은 것은?
   a) 중복 튜플 중 일부만 변경하여 데이터가 불일치하게 되는 문제입니다.
   b) 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입해야 하는 문제입니다.
   c) 튜플을 삭제하면 꼭 필요한 데이터까지 삭제되는 데이터 손실의 문제입니다.
   d) 테이블 간의 연관관계가 존재하지 않아 데이터의 일관성이 유지되지 않는 문제입니다.

6. 테이블 구조를 변경하기 위한 명령어는 무엇인가요?
   a) ALTER TABLE
   b) MODIFY TABLE
   c) CHANGE TABLE
   d) UPDATE TABLE

7. 컬럼을 추가하기 위한 ALTER TABLE 구문은 어떻게 작성하나요?
   a) ALTER TABLE 테이블명 ADD 컬럼명 데이터유형;
   b) ALTER TABLE 테이블명 MODIFY 컬럼명 데이터유형;
   c) ALTER TABLE 테이블명 DROP 컬럼명;
   d) ALTER TABLE 테이블명 RENAME TO 새테이블명;

8. 컬럼을 삭제하기 위한 ALTER TABLE 구문은 어떻게 작성하나요?
   a) ALTER TABLE 테이블명 ADD 컬럼명 데이터유형;
   b) ALTER TABLE 테이블명 MODIFY 컬럼명 데이터유형;
   c) ALTER TABLE 테이블명 DROP 컬럼명;
   d) ALTER TABLE 테이블명 RENAME TO 새테이블명;

9. 테이블 이름을 변경하기 위한 ALTER TABLE 구문은 어떻게 작성하나요?
   a) ALTER TABLE 테이블명 ADD 컬럼명 데이터유형;
   b) ALTER TABLE 테이블명 MODIFY 컬럼명 데이터유형;
   c) ALTER TABLE 테이블명 DROP 컬럼명;
   d) ALTER TABLE 테이블명 RENAME TO 새테이블명;

10. 테이블에 기본 키를 추가하기 위한 ALTER TABLE 구문은 어떻게 작성하나요?
    a) ALTER TABLE 테이블명 ADD CONSTRAINT 제약조건명 PRIMARY KEY (컬럼명);
    b) ALTER TABLE 테이블명 MODIFY 컬럼명 데이터유형;
    c) ALTER TABLE 테이블명 DROP 컬럼명;
    d) ALTER TABLE 테이블명 RENAME TO 새테이블명;

11. 클라이언트에서 서버로 데이터를 전송하기 위해 사용하는 객체는 무엇인가요?
   a) Request 객체
   b) Response 객체
   c) Session 객체
   d) Cookie 객체

12. query string은 무엇을 의미하나요?
   a) 클라이언트에서 서버로 전송되는 데이터의 형식을 나타내는 문자열입니다.
   b) 서버에서 클라이언트로 전송되는 데이터의 형식을 나타내는 문자열입니다.
   c) 클라이언트에서 서버로 전송되는 데이터를 포함한 URL 문자열입니다.
   d) 서버에서 클라이언트로 전송되는 데이터를 포함한 URL 문자열입니다.

13. request.getParameter("key") 메서드의 역할은 무엇인가요?
   a) key에 해당하는 요청값을 가져옵니다.
   b) key에 해당하는 응답값을 가져옵니다.
   c) key에 해당하는 쿠키 정보를 가져옵니다.
   d) key에 해당하는 세션 정보를 가져옵니다.

14. request.getParameterValues("key1") 메서드의 역할은 무엇인가요?
   a) key1에 해당하는 요청값들을 배열로 가져옵니다.
   b) key1에 해당하는 응답값들을 배열로 가져옵니다.
   c) key1에 해당하는 쿠키 정보들을 배열로 가져옵니다.
   d) key1에 해당하는 세션 정보들을 배열로 가져옵니다.

15. request.getParameter("key") 메서드를 사용하여 받은 요청값은 기본적으로 어떤 데이터 타입인가요?
   a) 문자열 (String)
   b) 정수형 (Integer)
   c) 실수형 (Double)
   d) 논리형 (Boolean)
   
   
1. 정규화의 목적은 무엇인가요?
   a) 데이터 중복을 최소화하고 데이터 일관성을 유지하기 위함입니다.
   b) 데이터를 효율적으로 저장하기 위함입니다.
   c) 데이터의 보안을 강화하기 위함입니다.
   d) 데이터를 정렬하고 검색하기 위함입니다.

1. 정답: a) 데이터 중복을 최소화하고 데이터 일관성을 유지하기 위함입니다.
   해설: 정규화의 주요 목적은 데이터 중복을 최소화하고 데이터 일관성을 유지하기 위해 데이터를 구조화하는 것입니다.
   초기에 구조자체가 데이터가 흠결이 없게 만들기 위한 목적이다.
   
2. 이상 현상 중에서 삽입 이상에 대한 설명으로 옳은 것은?
   a) 중복 튜플 중 일부만 변경하여 데이터가 불일치하게 되는 문제입니다.
   b) 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입해야 하는 문제입니다.
   c) 튜플을 삭제하면 꼭 필요한 데이터까지 삭제되는 데이터 손실의 문제입니다.
   d) 테이블 간의 연관관계가 존재하지 않아 데이터의 일관성이 유지되지 않는 문제입니다.

2. 정답: b) 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입해야 하는 문제입니다.
   해설: 삽입 이상은 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입해야 하는 문제를 말합니다.
   empdept 에서 사원정보만 입력시에도 부서정보가 null 입력해야 되는 현상.
   부서정보를 입력시, 사원정보가 null 처리나 default 입력..
   
   
   
3. 정규화 과정은 무엇을 의미하나요?
   a) 데이터의 중복을 최소화하면서 데이터베이스를 무결성있게 설계하는 과정입니다.
   b) 데이터를 효율적으로 저장하는 과정입니다.
   c) 데이터의 보안을 강화하는 과정입니다.
   d) 데이터를 정렬하고 검색하는 과정입니다.

3. 정답: a) 데이터의 중복을 최소화하면서 데이터베이스를 무결성있게 설계하는 과정입니다.
   해설: 정규화 과정은 데이터의 중복을 최소화하면서 데이터베이스를 무결성있게 설계하는 과정을 의미합니다.

4. 이상 현상 중에서 갱신 이상에 대한 설명으로 옳은 것은?
   a) 중복 튜플 중 일부만 변경하여 데이터가 불일치하게 되는 문제입니다.
   b) 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입해야 하는 문제입니다.
   c) 튜플을 삭제하면 꼭 필요한 데이터까지 삭제되는 데이터 손실의 문제입니다.
   d) 테이블 간의 연관관계가 존재하지 않아 데이터의 일관성이 유지되지 않는 문제입니다.

4. 정답: a) 중복 튜플 중 일부만 변경하여 데이터가 불일치하게 되는 문제입니다.
   해설: 갱신 이상은 중복 튜플 중 일부만 변경하여 데이터가 불일치하게 되는 문제를 말합니다.
   ex) empdept에서 부서명을 변경시, 오타나 실수로 다른 데이터 입력시
   다른 데이터와 동일하지 않는 현상 발생..
   
   

5. 이상 현상 중에서 삭제 이상에 대한 설명으로 옳은 것은?
   a) 중복 튜플 중 일부만 변경하여 데이터가 불일치하게 되는 문제입니다.
   b) 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입해야 하는 문제입니다.
   c) 튜플을 삭제하면 꼭 필요한 데이터까지 삭제되는 데이터 손실의 문제입니다.
   d) 테이블 간의 연관관계가 존재하지 않아 데이터의 일관성이 유지되지 않는 문제입니다.

5. 정답: c) 튜플을 삭제하면 꼭 필요한 데이터까지 삭제되는 데이터 손실의 문제입니다.
   해설: 삭제 이상은 튜플을 삭제하면 꼭 필요한 데이터까지 삭제되는 데이터 손실의 문제를 말합니다.
   empdept테이블 사원정보, 부서정보
   	하나만 삭제 하더라도 둘다 삭제.. 
   	

6. 테이블 구조를 변경하기 위한 명령어는 무엇인가요?
   a) ALTER TABLE
   b) MODIFY TABLE
   c) CHANGE TABLE
   d) UPDATE TABLE

6. 정답: a) ALTER TABLE
   해설: 테이블 구조를 변경하기 위한 명령어는 ALTER TABLE 입니다.

7. 컬럼을 추가하기 위한 ALTER TABLE 구문은 어떻게 작성하나요?
   a) ALTER TABLE 테이블명 ADD 컬럼명 데이터유형;
   b) ALTER TABLE 테이블명 MODIFY 컬럼명 데이터유형;
   c) ALTER TABLE 테이블명 DROP 컬럼명;
   d) ALTER TABLE 테이블명 RENAME TO 새테이블명;

7. 정답: a) ALTER TABLE 테이블명 ADD 컬럼명 데이터유형;
   해설: 컬럼을 추가하기 위한 ALTER TABLE 구문은 "ALTER TABLE 테이블명 ADD 컬럼명 데이터유형;" 형식으로 작성합니다.


8. 컬럼을 삭제하기 위한 ALTER TABLE 구문은 어떻게 작성하나요?
   a) ALTER TABLE 테이블명 ADD 컬럼명 데이터유형;
   b) ALTER TABLE 테이블명 MODIFY 컬럼명 데이터유형;
   c) ALTER TABLE 테이블명 DROP 컬럼명;
   d) ALTER TABLE 테이블명 RENAME TO 새테이블명;



8. 정답: c) ALTER TABLE 테이블명 DROP 컬럼명;
   해설: 컬럼을 삭제하기 위한 ALTER TABLE 구문은 "ALTER TABLE 테이블명 DROP 컬럼명;" 형식으로 작성합니다.



9. 테이블 이름을 변경하기 위한 ALTER TABLE 구문은 어떻게 작성하나요?
   a) ALTER TABLE 테이블명 ADD 컬럼명 데이터유형;
   b) ALTER TABLE 테이블명 MODIFY 컬럼명 데이터유형;
   c) ALTER TABLE 테이블명 DROP 컬럼명;
   d) ALTER TABLE 테이블명 RENAME TO 새테이블명;

9. 정답: d) ALTER TABLE 테이블명 RENAME TO 새테이블명;
   해설: 테이블 이름을 변경하기 위한 ALTER TABLE 구문은 "ALTER TABLE 테이블명 RENAME TO 새테이블명;" 형식으로 작성합니다.

10. 테이블에 기본 키를 추가하기 위한 ALTER TABLE 구문은 어떻게 작성하나요?
    a) ALTER TABLE 테이블명 ADD CONSTRAINT 제약조건명 PRIMARY KEY (컬럼명);
    b) ALTER TABLE 테이블명 MODIFY 컬럼명 데이터유형;
    c) ALTER TABLE 테이블명 DROP 컬럼명;
    d) ALTER TABLE 테이블명 RENAME TO 새테이블명;

10. 정답: a) ALTER TABLE 테이블명 ADD CONSTRAINT 제약조건명 PRIMARY KEY (컬럼명);
    해설: 테이블에 기본 키를 추가하기 위한 ALTER TABLE 구문은 "ALTER TABLE 테이블명 ADD CONSTRAINT 제약조건명 PRIMARY KEY (컬럼명);" 형식으로 작성합니다.
    ALTER TABLE 테이블명 MODIFY 컬럼명 primary key; 가능..

11. 클라이언트에서 서버로 데이터를 전송하기 위해 사용하는 객체는 무엇인가요?
   a) Request 객체
   b) Response 객체 : 서버에서 클라이언트 전송시 사용하는 객체
   c) Session 객체 : 서버와 클라이언트 간의 임시 메모리를 사용할 때 사용하는 객체
   		            request.getSession()
   d) Cookie 객체 : 클라이언트에 쿠키 정보로 
                    request.getCookies()

11. 정답: a) Request 객체
    해설: 클라이언트에서 서버로 데이터를 전송하기 위해 사용하는 객체는 Request 객체입니다.
    client ==> server  : query string으로 전송
    서버단에서 프로그램적으로 HttpServletRequest request에 의해서
       

12. query string은 무엇을 의미하나요?
   a) 클라이언트에서 서버로 전송되는 데이터의 형식을 나타내는 문자열입니다.
   b) 서버에서 클라이언트로 전송되는 데이터의 형식을 나타내는 문자열입니다.
   c) 클라이언트에서 서버로 전송되는 데이터를 포함한 URL 문자열입니다.
   d) 서버에서 클라이언트로 전송되는 데이터를 포함한 URL 문자열입니다.

12. 정답: c) 클라이언트에서 서버로 전송되는 데이터를 포함한 URL 문자열입니다.
    해설: query string은 클라이언트에서 서버로 전송되는 데이터를 포함한 URL 문자열을 의미합니다.
    query string 전달 방식
    1. get : url에 전달페이지와 함께 표시
    2. post : url에 보이지 않지만 내부적으로 전달.
    

13. request.getParameter("key") 메서드의 역할은 무엇인가요?
   a) key에 해당하는 요청값을 가져옵니다.
   b) key에 해당하는 응답값을 가져옵니다.
   c) key에 해당하는 쿠키 정보를 가져옵니다.
   d) key에 해당하는 세션 정보를 가져옵니다.

13. 정답: a) key에 해당하는 요청값을 가져옵니다.
    해설: request.getParameter("key") 메서드는 key에 해당하는 요청값을 가져옵니다.

14. request.getParameterValues("key1") 메서드의 역할은 무엇인가요?
   a) key1에 해당하는 요청값들을 배열로 가져옵니다.
   b) key1에 해당하는 응답값들을 배열로 가져옵니다.
   c) key1에 해당하는 쿠키 정보들을 배열로 가져옵니다.
   d) key1에 해당하는 세션 정보들을 배열로 가져옵니다.

14. 정답: a) key1에 해당하는 요청값들을 배열로 가져옵니다.
    해설: request.getParameterValues("key1") 메서드는 key1에 해당하는 요청값들을 배열로 가져옵니다.
    동일한 이름의 요청값을 가져올 때 사용.
    &key1=val1&key1=val2&key1=val3
    
    
    
15. request.getParameter("key") 메서드를 사용하여 받은 요청값은 기본적으로 어떤 데이터 타입인가요?
   a) 문자열 (String)
   b) 정수형 (Integer)
   c) 실수형 (Double)
   d) 논리형 (Boolean)

15. 정답: a) 문자열 (String)
    해설: request.getParameter("key") 메서드를 사용하여 받은 요청값은 기본적으로 문자열(String) 형태입니다.
    
    client :
    	query string 클라이언트 단에서 요청할 때 사용한다.
    	js : location.href="@@@jsp?key=val";
    	a tag : <a href="@@@jsp?key=val">문자열
    server
    	request.getParameter("key");
    	예외) 서버단에서 보내게 할때(클라이언트단에 가서 다시 서버단 호출)
    	response.sendRedirect("@@@jsp?key=val");	

    
    
    

 
   
   
   

   

