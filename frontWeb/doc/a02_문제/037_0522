1. 문제: 자바스크립트에서 에러 트래킹을 할 때, 실행하는 코드 중간에 단계별로 numbering해서 프로세스가 잘 진행되는지 확인하는 명령어는 무엇인가요?
    - A. console.count()
    - B. console.log() #
    - C. console.trace()
    - D. console.error()

2. 문제: 자바스크립트에서 `undefined` 상태는 어떤 상황을 의미하는가?
    - A. 변수가 선언되었으나, 값이 할당되지 않은 상태 #
    - B. 변수가 선언되지 않은 상태
    - C. 변수에 null 값이 할당된 상태
    - D. 변수가 이전에 선언되었으나 현재는 삭제된 상태
	var num01;
	console.log(num01)
	undefined, NaN :실행에러 - 다음 라인 수행
3. 문제: 자바스크립트에서 사용자에게 메시지를 보여주고 참거짓 값을 반환하는 입력 방법은 무엇인가요?
    - A. alert("메시지")
    - B. confirm("메시지") # true/false
    - C. prompt("메시지","입력초기") #입력값/null
    - D. console.log("메시지")

4. 문제: 자바스크립트에서 선언된 변수의 데이터 유형을 확인하는 명령어는 무엇인가요?
    - A. confirm(변수명)
    - B. console.log(변수명)
    - C. typeof(변수명) #
    - D. check(변수명)

5. 문제: 자바스크립트에서 문자열과 숫자를 비교하여 둘 다 같으면 true를 반환하는 연산자는 무엇인가요?
    - A. === : 내용과 데이유형을 비교해서 둘다 true
    	     "25" === "25" true
    	     "25" === 25   false
    	     25 === 25     true
    - B. == # :  "25" == 25 문자열과 숫자를 비교하여 같으면 
    - C. &&
    - D. ||
6. 문제: 자바스크립트에서 중첩 조건문을 표현하기 위해 사용하는 구문은 어떤 것인가요?
    - A. if...else if #
    		if
    			if
    - B. if...then
    - C. if...and
    - D. if...or

7. 문제: 자바스크립트에서 조건에 따라 값을 결정하는 3항 연산자는 어떤 형태인가요?
    - A. (조건) ? (true일 때 처리) : (false일 때 처리) #
    - B. (조건) && (true일 때 처리) || (false일 때 처리)
    - C. if (조건) { true일 때 처리 } else { false일 때 처리 }
    - D. (조건) & (true일 때 처리) | (false일 때 처리)

8. 문제: 자바스크립트에서 for 반복문을 사용하여 배열의 인덱스와 값을 각각 출력하는 방법은 무엇인가요?
    - A. for(let i of 배열){ 배열[i] }
    - B. for(let i in 배열){ 배열[i] } #
    		i :  index명
    		배열[i] : 해당 데이터의 값...
    - C. 배열.forEach(i => {});
         배열.forEach(data, i => {});
         배열.forEach(function(obj, idx,arry){})
9. 자바스크립트에서 에러 트래킹을 위해 사용하는 명령어는 무엇인가요?
    A. alert()
    B. console.log() #
    C. document.write()
    D. confirm()

10. 자바스크립트에서 브라우저 화면의 콘솔창을 확인하는 단축키는 무엇인가요?
    A. ctrl + F
    B. F12 #
    C. alt + F4
    D. F5  : 에러에 대한 코드 변경 후, 다시 확인 확인 

11. 자바스크립트에서 입력을 받는 명령어 중 하나인 prompt의 반환 값 유형은 무엇인가요?
    A. 문자열 또는 null #  확인시 문자열, 취소 클릭시 null 
    B. 숫자
    C. 배열
    D. Boolean

12. 자바스크립트에서 데이터 유형을 확인하기 위한 명령어는 무엇인가요?
    A. console.log()
    B. isNaN()  Is not a number? 숫자 아닐 때, true 처리
                if(isNaN(변수명)){}
    C. typeof()  #
    D. confirm()

13. 다음 중 자바스크립트에서 사용되는 데이터 유형이 아닌 것은 무엇인가요?
    A. Number
    B. Array   var arry = new Array() : 생성자로 사용
    		   typeof(arry) : object
    C. String
    D. Table #

14. 다음 중 자바스크립트에서 제공하는 산술 연산자가 아닌 것은 무엇인가요?
    A. *
    B. -
    C. &&  논리연산자 ps) 데이터 == 데이터 : 비교연산자
    D. %

15. 다음 중 자바스크립트에서 동등 연산자로 알려진 것은 무엇인가요?
    A. == #: 데이터 값의 내용만 비교 동등 연산자.
    B. = : 대입
    C. === : 데이터 타입과 내용을 비교 일치 연산자(js만 있음)
    D. != : 비교

16. 자바스크립트에서 배열의 값에 접근하기 위해 사용되는 방법은 무엇인가요?
    A. 배열명[인덱스] #
    B. 배열명.인덱스
    C. 배열명(인덱스)
    D. 배열명{인덱스}

17. 자바스크립트에서 반복문 중 조건이 먼저 확인되는 반복문은 무엇인가요?
	조건이 먼저 나오는 것을 선택
    A. for  for(초기값;반복조건;증감연산자)
    B. while# while(조건)
    C. do while do{ 실행 후}while(조건)
    D. forEach 배열.forEach(function(단위데이터,index,배열))
    

18. 다음 중 자바스크립트에서 사용자 정의 함수를 선언하는 방식이 아닌 것은 무엇인가요?
    A. var 함수명 = function(){} 변수형 함수
    B. function 함수명(){} 선언적 함수
    C. let 함수명 = function(){} 변수형 함수
    D. var 함수명 = new function(){} 함수 ==> 객체
        function 객체명(){
        	this.속성 = 속성값;
        }
    	var 객체명 = new 생성자();

19. 자바스크립트에서 배열을 선언하는 방식 중 올바른 것은 무엇인가요?
    A. var 배열명 = {데이터1, 데이터2, ...}
    B. var 배열명 = [데이터1, 데이터2, ...] #
    C. var 배열명 = (데이터1, 데이터2, ...)
    D. var 배열명 = <데이터1, 데이터2, ...>

