# 오전 문제
1. 자바스크립트에서 객체를 문자열로 변환하는 함수는 무엇인가요?
    a) JSON.parse()
    b) JSON.stringify() #
    c) JSON.objectify()
    d) JSON.toString()
    웹에서 네트웍상 데이터를 전송할 때, 사용되는 형식
    client ==> 문자열객체  ==> server(java) lib gson
    객체                     객체
    client <== 문자열객체  <== server(java)
    객체                     객체 
    
    JSON.parse() : 객체형 문자열 ==> 객체
    JSON.stringify() : 객체 ==> 객체형 문자열
       

2. 문자열을 JSON 객체로 변환하는 함수는 무엇인가요?
    a) JSON.parse() #
    b) JSON.stringify()
    c) JSON.objectify()
    d) JSON.toString()

3. JSON 데이터 형식에서 속성 값에는 어떤 구분자를 사용해야 하는가요?
    a) ''
    b) "" #
    c) ``
    d) None of the above
    속성은 ""사이에 선언
    속성값은 문자열데이터인 경우 ""사이, 숫자형/boolean 그대로 사용

4. 자바스크립트에서 이벤트를 처리하는 기본 형식 중 하나는 무엇인가요?
    a) <태그 on이벤트명="이벤트핸들러함수()"> #
    b) dom객체.on이벤트명()
    c) 이벤트명.dom객체()
    d) dom객체=이벤트명()
    
    #이벤트 처리형식
    1. <태그 on이벤트명="이벤트핸들러함수()">
    2. dom객체.on이벤트명 = 함수
    3. dom객체.on이벤트명 = function(){
       }
    

5. 키를 눌렀다가 떼었을 때 발생하는 이벤트는 무엇인가요?
    a) keydown
    b) keyup #
    c) keypress
    d) keyhold
    keydown : 키를 누르고 있는 상태에서 발생하는 이벤트
    keyup : 키를 눌렀다고 떼었을 때 발생하는 이벤트.
    

6. form 데이터를 처리하기 전에 유효성을 확인하는 이벤트 핸들러는 어디에 선언하는가요?
    a) <form onsubmit="이벤트 핸들러"> #
    b) <form onsubmit>
    c) <form action="이벤트 핸들러">
    d) <form method="이벤트 핸들러">
    # 서버에 전송전에 유효성 처리방법
    1. <form onsubmit="return 이벤트핸들러함수()">
          function 이벤트핸들러함수(){
        	  if(유효성맞지 않는 조건){
       	   	      return false; // 프로세스가 중단..
       	      }             
          }
    2. <form>
          <input type="submit"
          ==> 
          <input type="button" onclick="이벤트핸들러함수()"
       function 이벤트핸들러함수(){
       	   유효성 check
       	   if(유효성맞지 않는 조건){
       	   	  return; // 프로세스가 중단..
       	   }
       	   ..
       	   ..
       	   // 강제 이벤트 수행 처리...
       	   document.querySelector("form").submit();
       }   
          
    
    
    
    

7. 'name'이라는 속성을 가진 입력 필드에 접근하는 적절한 코드는 무엇인가요?
    a) document.querySelector("[name=name]") #
    b) document.querySelector("name")
    c) document.querySelector("{name}")
    d) document.querySelector("#name")
    
    css선택자 형식을 유지한다.
    <요소객체 속성=속성값 >
    document.querySelector("[속성=속성값]")

8. 서버에 데이터를 전송하기 전에 필요한 유효성 검사 중 하나는 무엇인가요?
    a) 필드 값이 비어 있는지 확인 #
    b) 필드 값이 숫자인지 확인 #
    c) 필드 값이 JSON 형식인지 확인
    d) 필드 값이 문자열인지 확인
    
    서버에 전송전에 데이터에 대한 유효데이터 형식에 대한 처리를
    하여야지 서버에서의 에러를 방지 할 수 있다.
	    비어 있을 때  if(val=="")
	    숫자형이어야할 때 if( isNaN(val) )
	      isNaN : Is not a number? 숫자형이 아닐 때, true
	    데이터의 크기, 형식(정규표형식과 메서드)로 check해서
	    전송할 수 있다.  

9. 이벤트 핸들러에서 'return false;'의 역할이 무엇인가요?
    a) 함수의 실행을 중지
    b) 이벤트 전파를 막음
    c) 이벤트의 기본 작업을 취소 #
    d) 이벤트 핸들러를 삭제
   	function 이벤트핸들러함수(){
   		if(유효하지않을 조건){
   			return false;
   		}
   	}
   	// 해당 이벤트이 기본 작업을 취소하는 역할을 해준다.
   	// 1. 이벤트 전파을 막는 작업도 포함..
   	// 2. 함수의 실행을 중지  	
    <form onsubmit="return 이벤트핸들러함수()"
    
    

10. JSON 데이터 형식에서 속성 값을 선언하는 올바른 방법은 무엇인가요?
    a) "속성":"문자열" #
    b) '속성':'문자열'
    c) 속성:"문자열"
    d) "속성":'문자열'
    문자열로 json데이터를 선언할 반드시 지켜주어야할 형식
    `{"속성":"문자열데이터", "속성":숫자/boolean형}`
    ex) `{"name":"홍길동","age":25}` 
    
11. 아래의 코드에서 잘못된 부분은 무엇인가?
    var p01Str2 = '{'name':'홍길동','age':25}'
    A. 작은따옴표를 사용한 것이 잘못됨
    B. 객체의 키와 값이 올바르지 않음 #
    C. 변수 이름이 잘못됨
    D. 아무런 문제가 없음
    var p01Str2 = '{"name":"홍길동","age":25}'
    // json 문자열 데이터 형식
    var p01 = {name:"홍길동",age:25}
    // json 객체형식1
    var p01 = {name:'홍길동',age:25}
    // json 객체형식2
    var p01 = {'name':'홍길동','age':25}
    var p01 = {"name":'홍길동','age':25}
    // json 객체형식3

12. 웹프로그래밍에서 '이벤트'는 무엇을 가리키는가?
    A. 사용자의 마우스 클릭 #
    B. 데이터베이스에 저장되는 정보
    C. 서버에서 클라이언트로의 응답
    D. 웹사이트의 로딩 속도
    # 이벤트 처리시 필요 요소
    1. 이벤트대상 객체(DOM)
    2. 이벤트
    3. 이벤트핸들러함수

13. JSON 객체를 네트워크를 통해 전송하려면 어떤 과정을 거쳐야 하는가?
    A. 객체를 문자열로 변환#
    B. 객체를 숫자로 변환
    C. 객체를 함수로 변환
    D. 변환 없이 그대로 전송

14. form 태그의 onsubmit 이벤트는 언제 발생하는가?
    A. 사용자가 form 안의 'submit' 타입 버튼을 눌렀을 때 #
    B. 사용자가 form 안의 입력 필드에 데이터를 입력했을 때
    C. form 태그가 로드될 때
    D. 사용자가 form 안의 'reset' 타입 버튼을 눌렀을 때
    <form action="서버의자원"
    	<input type="submit" : form포함된 모든 요소객체를
    		name="key" value="val" 서버에 전송	
    	<input type="reset" : form에 있는 모든 요소를 초기화시

15. 아래 코드에서 ckSendData 함수의 반환 값이 false일 때, 어떤 상황이 발생하는가?
    `<form onsubmit="return ckSendData();">`
    A. 폼의 제출이 중지됨 #
    B. 폼의 제출이 계속됨
    C. 폼의 모든 필드가 초기화됨
    D. ckSendData 함수가 다시 호출됨
     
16. 자바스크립트에서 정규표현식의 사용방법에 대한 설명으로 올바른 것은 무엇인가요?

A) 정규표현식은 문자열 내에서 특정한 패턴을 찾거나 대치하는데 사용됩니다.#
B) 정규표현식은 숫자와 기호만으로 표현됩니다.
C) 정규표현식은 HTML과 CSS에서만 사용됩니다.
D) 정규표현식은 문자열의 길이를 체크할 때 사용됩니다.

1. 정규표현식을 활용하여 전송데이터의 유효성을 처리할 때 가장 많이 활용된다.
2. split() 데이터를 구분자에 의해 처리할 때..


17. JavaScript에서 정규 표현식을 통해 특정 문자열을 대체하는 메소드는 무엇인가요?
A) exec()
B) test()
C) replace() #
D) match()

18. 자바스크립트에서 문자열을 분해하여 배열로 반환하는 메소드는 무엇인가요?

A) split() #
B) join()
C) push()
D) pop()

19. 정규 표현식에서 패턴의 일치 여부를 확인하는 메소드는 무엇인가요?

A) check()
B) test() #
C) match()
D) search()
test() 유효성 여부
match() 검색데이터를 배열로 리턴
search() 검색위치를 index로 리턴

20. 다음 중 자바스크립트의 정규식 패턴에서 /g 플래그의 역할에 대한 설명으로 올바른 것은 무엇인가요?

A) 대소문자를 구분하지 않고 검색합니다.
B) 전역에서 일치하는 모든 결과를 반환합니다. #
C) 한 개 이상의 일치하는 문자열을 반환합니다.
D) 일치하는 첫 번째 문자열만 반환합니다.
/g : 모든 내용 일치 결과를 확인
/i : 대소문자 상관없이 처리
/m : 여러줄에 데이터가 있을 때..
/gim : 혼합가능






       