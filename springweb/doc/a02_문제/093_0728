1. 스프링에서 개발 순서 중, json 데이터를 처리하는 Controller 기능 메서드는 어떤 순서로 선언되나요?
    1) view(jsp) ==> controller(json데이터로 처리)
    2) controller(json데이터로 처리) ==> service ==> dao
    3) dao ==> service ==> controller(json데이터로 처리)
    4) service ==> controller(json데이터로 처리) ==> dao


2. 화면에서 ajax로 입력된 내용을 controller 단에 호출하는 경우, 어떤 형식의 데이터가 리턴값으로 전달되나요?
    1) xml 데이터
    2) json 데이터
    3) 성공 여부
    4) 모두 다



3. 직책 정보 form 형식 등록 처리의 첫 번째 단계는 무엇인가요?
    1) 직책리스트에서 등록화면 버튼 클릭시, 등록화면 이동
    2) 등록화면에서 직책아이디, 직책명, 최소급여, 최대급여 등록
    3) 등록 버튼 클릭시, 등록 성공 계속 여부 확인 메인 페이지 이동
    4) 모두 다

4. 스프링에서의 ajax 처리에 대한 설명 중 다음 중 옳지 않은 것은?
    1. 스프링은 여러가지 형식의 ajax 처리를 할 수 있다.
    2. 스프링은 jquery, vue, react등 여러 front단에서 요청한 ajax를 처리하지 못한다.
    3. container에 json viewer를 처리를 할 수 있게 처리가 가능하다.
    4. "json뷰어"를 통해 ajax 데이터를 가져올 수 있다.

5. 스프링에서의 ajax 처리 개발 순서 중 옳지 않은 것은?
    1. 초기화면 로딩은 ctrl ==> view(jsp)의 순서로 이루어진다.
    2. json데이터를 처리한 controller 기능메서드가 선언된다.
    3. 이벤트에 의해 ajax처리가 이루어진다.
    4. 이벤트에 의해 초기화면 로딩이 이루어진다.

6. 스프링에서의 ajax 처리는 front단에서 요청한 ajax를 backend에서 어떻게 처리하는가?
    1. XML 데이터를 효율적으로 처리한다.
    2. JSON 데이터를 효율적으로 처리한다.
    3. HTML 데이터를 효율적으로 처리한다.
    4. 모든 데이터 형식을 효율적으로 처리한다.

7. 다음 중 스프링에서 ajax 요청 처리의 기본 형태를 가장 잘 설명한 것은 무엇인가요?
    - A) ajax 요청을 모델 데이터로 변환하여 처리한다.
    - B) "json뷰어"를 통해 ajax 데이터를 가져올 수 있다.
    - C) container에 json viewer 처리를 할 수 있게 한다.
    - D) 자바 객체를 json 형식의 객체로 변환한다.

8. 스프링에서 화면처리는 어떻게 이루어지는가요?
    - A) 컨트롤러에서 전달된 JSON 데이터를 화면단에 출력한다.
    - B) 직접 HTML 코드를 작성하여 화면을 구성한다.
    - C) AJAX를 이용하여 서버와 통신하며 데이터를 업데이트한다.
    - D) 모든 답변이 정확하다.

9. '등록 입력 값 처리' 단계에서 등록된 내용을 클릭하면 어떤 동작이 이루어지는가요?
    - A) 등록창이 닫힌다.
    - B) 등록된 내용이 데이터베이스로 전송된다.
    - C) 등록된 내용이 ajax로 controller단에 전달된다.
    - D) 등록창에서 새로운 내용을 입력할 수 있다.


1. 스프링에서 개발 순서 중, json 데이터를 처리하는 Controller 기능 메서드는 어떤 순서로 선언되나요?
    1) view(jsp) ==> controller(json데이터로 처리)
    2) controller(json데이터로 처리) ==> service ==> dao
    3) dao ==> service ==> controller(json데이터로 처리)
    4) service ==> controller(json데이터로 처리) ==> dao

정답: 3) dao ==> service ==> controller(json데이터로 처리)
해설: JSON 데이터를 처리하는 Controller 기능 메서드는 일반적으로 Controller에서 시작하여 Service를 거쳐 DAO(Data Access Object)까지 처리하는 순서로 선언됩니다.


2. 화면에서 ajax로 입력된 내용을 controller 단에 호출하는 경우, 어떤 형식의 데이터가 리턴값으로 전달되나요?
    1) xml 데이터
    2) json 데이터
    3) 성공 여부
    4) 모두 다

정답: 2) json 데이터
해설: 화면에서 Ajax로 호출한 경우, 주로 JSON 데이터가 리턴값으로 전달됩니다. JSON은 데이터를 자바스크립트 객체 형식으로 표현하는데 자주 사용되는 형식이며, Ajax를 통해 서버로부터 데이터를 비동기적으로 받아오기 위해 주로 사용됩니다.

3. 직책 정보 form 형식 등록 처리의 첫 번째 단계는 무엇인가요?
    1) 직책리스트에서 등록화면 버튼 클릭시, 등록화면 이동
    2) 등록화면에서 직책아이디, 직책명, 최소급여, 최대급여 등록
    3) 등록 버튼 클릭시, 등록 성공 계속 여부 확인 메인 페이지 이동
    4) 모두 다

정답: 1) 직책리스트에서 등록화면 버튼 클릭시, 등록화면 이동
해설: 직책 정보 등록 처리의 첫 번째 단계는 직책 리스트에서 등록 화면으로 이동하는 단계입니다.

4. 스프링에서의 ajax 처리에 대한 설명 중 다음 중 옳지 않은 것은?
    1. 스프링은 여러가지 형식의 ajax 처리를 할 수 있다.
    2. 스프링은 jquery, vue, react등 여러 front단에서 요청한 ajax를 처리하지 못한다.
    3. container에 json viewer를 처리를 할 수 있게 처리가 가능하다.
    4. "json뷰어"를 통해 ajax 데이터를 가져올 수 있다.

정답: 2. 스프링은 jquery, vue, react 등 여러 front단에서 요청한 ajax를 처리하지 못한다.
해설: 옳지 않은 설명은 2번입니다. 스프링은 다양한 프론트엔드 기술에서 요청한 Ajax를 처리할 수 있습니다. 따라서 jQuery, Vue.js, React 등 다른 프론트엔드 프레임워크에서 요청한 Ajax도 스프링에서 처리가 가능합니다.


5. 스프링에서의 ajax 처리 개발 순서 중 옳지 않은 것은?
    1. 초기화면 로딩은 ctrl ==> view(jsp)의 순서로 이루어진다.
    2. json데이터를 처리한 controller 기능메서드가 선언된다.
    3. 이벤트에 의해 ajax처리가 이루어진다.
    4. 이벤트에 의해 초기화면 로딩이 이루어진다.

정답: 4. 이벤트에 의해 초기화면 로딩이 이루어진다.
해설: 옳지 않은 설명은 4번입니다. Ajax 처리 개발 순서에서 이벤트에 의해 초기화면 로딩은 일반적으로 처리되지 않습니다. 초기화면 로딩은 Ajax 처리와 별도로 이루어지는 것이 보통입니다.


6. 스프링에서의 ajax 처리는 front단에서 요청한 ajax를 backend에서 어떻게 처리하는가?
    1. XML 데이터를 효율적으로 처리한다.
    2. JSON 데이터를 효율적으로 처리한다.
    3. HTML 데이터를 효율적으로 처리한다.
    4. 모든 데이터 형식을 효율적으로 처리한다.

정답: 2. JSON 데이터를 효율적으로 처리한다.
해설: 스프링에서는 주로 JSON 데이터를 효율적으로 처리하여 Front-end에서 요청한 Ajax를 처리합니다.

7. 다음 중 스프링에서 ajax 요청 처리의 기본 형태를 가장 잘 설명한 것은 무엇인가요?
    - A) ajax 요청을 모델 데이터로 변환하여 처리한다.
    - B) "json뷰어"를 통해 ajax 데이터를 가져올 수 있다.
    - C) container에 json viewer 처리를 할 수 있게 한다.
    - D) 자바 객체를 json 형식의 객체로 변환한다.

정답: D) 자바 객체를 json 형식의 객체로 변환한다.
해설: 스프링에서 Ajax 요청 처리의 기본 형태는 자바 객체를 JSON 형식의 객체로 변환하여 데이터를 주고받는 것입니다.

8. 스프링에서 화면처리는 어떻게 이루어지는가요?
    - A) 컨트롤러에서 전달된 JSON 데이터를 화면단에 출력한다.
    - B) 직접 HTML 코드를 작성하여 화면을 구성한다.
    - C) AJAX를 이용하여 서버와 통신하며 데이터를 업데이트한다.
    - D) 모든 답변이 정확하다.

정답: D) 모든 답변이 정확하다.
해설: 스프링에서는 다양한 방법으로 화면처리를 이루어집니다. 주로 컨트롤러에서 전달된 JSON 데이터를 화면단에 출력하거나 직접 HTML 코드를 작성하여 화면을 구성하며, AJAX를 이용하여 서버와 통신하여 데이터를 업데이트할 수 있습니다.

9. '등록 입력 값 처리' 단계에서 등록된 내용을 클릭하면 어떤 동작이 이루어지는가요?
    - A) 등록창이 닫힌다.
    - B) 등록된 내용이 데이터베이스로 전송된다.
    - C) 등록된 내용이 ajax로 controller단에 전달된다.
    - D) 등록창에서 새로운 내용을 입력할 수 있다.

정답: C) 등록된 내용이 ajax로 controller 단에 전달된다.
해설: '등록 입력 값 처리' 단계에서 등록된 내용을 클릭하면 등록된 내용이 Ajax를 통해 Controller 단에 전달됩니다. 이후 해당 내용을 기반으로 추가적인 처리가 이루어질 수 있습니다.
