1. 웹 개발에서 데이터베이스 연동 처리의 중요성은 무엇인가?
   a. 웹 디자인을 향상시키기 위함
   b. 프로그램의 속도를 높이기 위함
   c. 프로그램에서 반드시 수행해야 하는 자원의 효율적 처리와 DB 연동을 가능하게 하기 위함
   d. 웹 개발의 복잡성을 높이기 위함

2. Mybatis framework은 무엇을 위해 사용되는가?
   a. 웹 디자인 향상
   b. DB 처리를 효과적으로 하기 위해
   c. 웹 개발 과정의 단순화
   d. 코드 최적화

4. Spring에서 사용되는 DAO 객체 호출 방식은 무엇인가?
   a. @Autowired
   b. @RequestMapping
   c. @Service
   d. @Controller

5. 서비스 계층에서 DAO 기능 메서드를 복사하는 이유는 무엇인가?
   a. 비즈니스 로직 처리를 위해
   b. DB 연결을 위해
   c. 화면단 구현을 위해
   d. 데이터 유효성 검사를 위해

6. 컨트롤러 만들기에서 요청 메서드 선언의 목적은 무엇인가?
   a. 요청값 처리
   b. 모델 데이터 처리
   c. url 선언
   d. 서비스 객체 선언

7. Mybatis framework 처리 학습 내용 계열화의 첫 번째 단계는 무엇인가?
    a. 인터페이스 개념 이해
    b. mybatis framework에 의해 처리 구조에 대한 이해
    c. dao 인터페이스와 mapper.xml의 설정에 대한 이해
    d. 실제 개발 순서 및 처리

8. mybatis framework를 사용해 실제 객체를 만들 때, mapper.xml의 네임스페이스의 역할은 무엇인가?
    a. DB 연결 설정
    b. sql 작성
    c. vo 작성
    d. 인터페이스 선언

9. 실제 개발 순서에서 sql 작성 후 다음 단계는 무엇인가?
    a. vo 작성
    b. dao 인터페이스 작성
    c. 공통 mybatis 등록
    d. mapper.xml 선언 및 처리

10. DB 연결 처리에서 DBCP(Database Connection Pool)를 사용하는 목적은 무엇인가?
    a. 코드 최적화
    b. 웹 디자인 향상
    c. DB 연결을 효율적으로 관리하기 위해
    d. 프로그램의 속도를 높이기 위해

11. Mybatis 연동 처리에서 mybatis 공통 xml 연동 처리의 목적은 무엇인가?
    a. 인터페이스 선언을 위해
    b. vo 작성을 위해
    c. sql 작성을 위해
    d. mybatis를 통해 만들어질 실제 객체가 바라보는 dao 인터페이스 위치 설정을 위해

12. Mybatis framework에서 mapper.xml의 id는 무엇을 나타내는가?
    a. 메서드명
    b. 네임스페이스
    c. 입력값
    d. 출력 데이터

13. 화면단 구현에 관한 아래 설명 중 잘못된 것은 무엇인가?
    a. 검색할 폼을 구현해야 한다.
    b. 리스트할 테이블을 구성해야 한다.
    c. HTML과 CSS를 사용한다.
    d. 요청값을 처리하지 않아도 된다.


# 정답 및 해설

1. 웹 개발에서 데이터베이스 연동 처리의 중요성은 무엇인가?
   a. 웹 디자인을 향상시키기 위함
   b. 프로그램의 속도를 높이기 위함
   c. 프로그램에서 반드시 수행해야 하는 자원의 효율적 처리와 DB 연동을 가능하게 하기 위함
   d. 웹 개발의 복잡성을 높이기 위함

1. c. 프로그램에서 반드시 수행해야 하는 자원의 효율적 처리와 DB 연동을 가능하게 하기 위함
   - 웹 애플리케이션은 데이터베이스에서 데이터를 검색, 추가, 수정 및 삭제하는 등 다양한 작업을 수행합니다. 이런 작업들을 가능하게 하기 위해 데이터베이스와 웹 애플리케이션을 연동하는 것이 필요합니다.


2. Mybatis framework은 무엇을 위해 사용되는가?
   a. 웹 디자인 향상
   b. DB 처리를 효과적으로 하기 위해
   c. 웹 개발 과정의 단순화
   d. 코드 최적화

2. b. DB 처리를 효과적으로 하기 위해
   - Mybatis는 개발자가 SQL, 저장 프로시저 및 고급 매핑을 직접 제어할 수 있는 퍼시스턴스 프레임워크입니다. 이를 통해 데이터베이스와의 상호작용을 보다 효율적으로 관리할 수 있습니다.


4. Spring에서 사용되는 DAO 객체 호출 방식은 무엇인가?
   a. @Autowired
   b. @RequestMapping
   c. @Service
   d. @Controller
4. a. @Autowired
   - Spring에서는 @Autowired 어노테이션을 사용하여 의존성 주입을 수행하며, 이를 통해 DAO 객체를 호출합니다.
	@Autowired
	private MemberDao dao;


5. 서비스 계층에서 DAO 기능 메서드를 복사하는 이유는 무엇인가?
   a. 비즈니스 로직 처리를 위해
   b. DB 연결을 위해
   c. 화면단 구현을 위해
   d. 데이터 유효성 검사를 위해

5. a. 비즈니스 로직 처리를 위해
   - 서비스 계층에서는 DAO의 메서드를 이용해 비즈니스 로직을 처리합니다. 이 로직은 데이터의 검증, 복잡한 처리 등을 포함할 수 있습니다.
   
   service단에서 처리
   요청값을 변경
   결과값을 변경
   할때 필요로 하는 if, for문 처리, 알고리즘 처리시 꼭필요한
   서비스 계층..
	


6. 컨트롤러 만들기에서 요청 메서드 선언의 목적은 무엇인가?
   a. 요청값 처리
   b. 모델 데이터 처리
   c. url 선언
   d. 서비스 객체 선언

6. a. 요청값 처리
   - 컨트롤러의 주요 역할 중 하나는 클라이언트의 요청을 받아 그에 따른 처리를 하는 것입니다. 이를 위해 요청 메서드를 선언합니다.

7. Mybatis framework 처리 학습 내용 계열화의 첫 번째 단계는 무엇인가?
    a. 인터페이스 개념 이해
    b. mybatis framework에 의해 처리 구조에 대한 이해
    c. dao 인터페이스와 mapper.xml의 설정에 대한 이해
    d. 실제 개발 순서 및 처리

7. b. mybatis framework에 의해 처리 구조에 대한 이해
   - Mybatis를 활용한 개발을 시작하기 전에, 그 처리 구조를 이해하는 것이 첫 번째 단계입니다.
	1) 실제 interface로 Dao가 처리되는 패턴에서는
		java에 대한 interface의 추상 = 실제 객체에 대한 이해가 
		필요로 한다.


8. mybatis framework를 사용해 실제 객체를 만들 때, mapper.xml의 네임스페이스의 역할은 무엇인가?
    a. DB 연결 설정
    b. sql 작성
    c. vo 작성
    d. 인터페이스 선언

8. d. 인터페이스 선언
   - mapper.xml 파일의 네임스페이스는 연관된 DAO 인터페이스를 지정합니다.



9. 실제 개발 순서에서 sql 작성 후 다음 단계는 무엇인가?
    a. vo 작성
    b. dao 인터페이스 작성
    c. 공통 mybatis 등록
    d. mapper.xml 선언 및 처리

9. DAO만드는 순서
	sql작성
	vo 작성
	dao 인터페이스 작성
	mapper 작성
	service단에서 테스트

	a.vo 작성

10. DB 연결 처리에서 DBCP(Database Connection Pool)를 사용하는 목적은 무엇인가?
    a. 코드 최적화
    b. 웹 디자인 향상
    c. DB 연결을 효율적으로 관리하기 위해
    d. 프로그램의 속도를 높이기 위해

10. c. DB 연결을 효율적으로 관리하기 위해
   - DBCP는 여러 사용자의 요청에 대해 동시에 처리할 수 있는 
   DB 연결을 효율적으로 관리하기 위해 사용됩니다.



11. Mybatis 연동 처리에서 mybatis 공통 xml 연동 처리의 목적은 무엇인가?
    a. 인터페이스 선언을 위해
    b. vo 작성을 위해
    c. sql 작성을 위해
    d. mybatis를 통해 만들어질 실제 객체가 바라보는 
        dao 인터페이스 위치 설정을 위해

11. d. mybatis를 통해 만들어질 실제 객체가 바라보는 dao 인터페이스 위치 설정을 위해
   - mybatis 공통 xml 연동 처리는 mybatis를 통해 만들어질 실제 객체가 바라보는 dao 인터페이스의 위치를 설정하는 것을 목적으로 합니다.



12. Mybatis framework에서 mapper.xml의 id는 무엇을 나타내는가?
    a. 메서드명
    b. 네임스페이스
    c. 입력값
    d. 출력 데이터

12. a. 메서드명
   - mapper.xml의 id는 연관된 DAO 인터페이스의 메서드명을 나타냅니다.

13. 화면단 구현에 관한 아래 설명 중 잘못된 것은 무엇인가?
    a. 검색할 폼을 구현해야 한다.
    b. 리스트할 테이블을 구성해야 한다.
    c. HTML과 CSS를 사용한다.
    d. 요청값을 처리하지 않아도 된다.

13. d. 요청값을 처리하지 않아도 된다.
   - 웹 페이지의 기능 중 하나는 사용자의 요청을 받아 처리하는 것입니다. 따라서 화면단 구현에서는 요청값을 처리해야 합니다.

