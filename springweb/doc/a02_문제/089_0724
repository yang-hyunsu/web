1. 동적 쿼리에서 반복문을 처리하기 위한 태그는 무엇인가요?
   - a) <if>
   - b) <choose>
   - c) <foreach>
   - d) <where>

2. 다음 중 스프링에서 파일업로드를 처리할 때 공통적으로 설정하는 객체를 컨테이너에 선언하는 것에 대한 설명이 가장 정확한 것은 무엇인가요?
   - a) 기본 파일 다운로드 위치 지정
   - b) 기본 파일 업로드 위치 지정
   - c) 기본 파일 수정 위치 지정
   - d) 기본 파일 삭제 위치 지정

3. 스프링에서 첨부파일을 네트워크를 통해 전송하여 받을 수 있는 객체는 무엇인가요?
   - a) File
   - b) MultipartRequest
   - c) MultipartFile
   - d) FileUpload

4. Mybatis에서는 동적 쿼리를 위하여 ${전달데이터}와 어떤 언어 방식을 지원하나요?
   - a) HTML
   - b) OGNL (Object Graph Navigation Language)
   - c) CSS
   - d) SQL

5. MyBatis의 resultMap에서 association 요소를 사용하는 이유는 무엇인가요?
   - a) 반복문을 처리하기 위해
   - b) 데이터를 삽입하기 위해
   - c) 객체간의 관계를 표현하기 위해
   - d) 데이터를 삭제하기 위해

6. 동적 쿼리에서 조건문을 처리하기 위한 태그는 무엇인가요?
   - a) <choose>
   - b) <if>
   - c) <foreach>
   - d) <where>

7. 스프링에서 파일 업로드 진행 프로세스 중, 등록된 파일이 특정 위치에 저장되고, DB에도 저장되는 단계는 무엇인가요?
   - a) 화면이 로딩되면 첨부 파일을 하는 항목이 있고
   - b) 해당 항목을 클릭을 통해 파일을 첨부한다.
   - c) 등록버튼을 통해서 해당 파일을 서버에 등록 처리한다.
   - d) 등록된 파일을 특정 위치에 저장되고, DB에도 저장이 된다.

8. Mybatis에서 <foreach> 태그의 'open' 속성이 하는 역할은 무엇인가요?
   - a) 반복형식의 시작을 표현한다.
   - b) 반복형식의 구분자를 표현한다.
   - c) 반복형식의 종료를 표현한다.
   - d) 반복형식의 단위데이터를 표현한다.

9. 동적 SQL을 만들어야 하는 주된 이유는 무엇인가요?
   - a) 데이터 처리의 효율성을 높이기 위해
   - b) 데이터의 변화에 따라 SQL의 변화가 필요한 경우
   - c) SQL의 구문 자체가 변경되는 것을 방지하기 위해
   - d) 모든 상황에 대해 동일한 SQL을 사용하기 위해

10. MyBatis에서 객체간의 관계를 표현하기 위해 어떤 요소를 사용하나요?
   - a) <choose>
   - b) <association>
   - c) <if>
   - d) <foreach>
   
1. 동적 쿼리에서 반복문을 처리하기 위한 태그는 무엇인가요?
   - a) <if>
   - b) <choose>
   - c) <foreach>
   - d) <where>
   
   정답: c) <foreach>
   이유: MyBatis에서는 <foreach> 태그를 사용하여 반복문을 처리할 수 있습니다. 이는 컬렉션의 각 항목에 대해 SQL 구문을 반복적으로 실행하는 데 사용됩니다.

2. 다음 중 스프링에서 파일업로드를 처리할 때 공통적으로 설정하는 객체를 컨테이너에 선언하는 것에 대한 설명이 가장 정확한 것은 무엇인가요?
   - a) 기본 파일 다운로드 위치 지정
   - b) 기본 파일 업로드 위치 지정
   - c) 기본 파일 수정 위치 지정
   - d) 기본 파일 삭제 위치 지정
   
   정답: b) 기본 파일 업로드 위치 지정
   이유: 스프링에서는 파일 업로드를 처리할 때, 파일이 업로드 될 
   기본 위치를 설정해야 합니다. 이 위치는 파일이 서버에 저장될 때 사용됩니다.
   브라우저에서 파일선택 ==> submit로 전송 ==> 
   서버에서 MultipartFile 파일로 받아서 ==> File객체로변환
   ==> 특정위치에 파일업로드..   
   
   

3. 스프링에서 첨부파일을 네트워크를 통해 전송하여 받을 수 있는 객체는 무엇인가요?
   - a) File
   - b) MultipartRequest
   - c) MultipartFile
   - d) FileUpload
   
   정답: c) MultipartFile
   이유: 스프링에서 파일 업로드를 처리할 때는 MultipartFile 인터페이스를 사용합니다. 이 인터페이스는 HTTP 요청에 첨부된 파일을 나타내며, 이를 사용하여 파일 데이터를 얻을 수 있습니다.

4. Mybatis에서는 동적 쿼리를 위하여 ${전달데이터}와 어떤 언어 방식을 지원하나요?
   - a) HTML
   - b) OGNL (Object Graph Navigation Language)
   - c) CSS
   - d) SQL
   
   정답: b) OGNL (Object Graph Navigation Language)
   이유: MyBatis는 동적 쿼리 생성을 위해 OGNL (Object Graph Navigation Language)를 사용합니다. 이는 객체의 속성을 탐색하고 조작하는데 사용되는 표현 언어입니다.

5. MyBatis의 resultMap에서 association 요소를 사용하는 이유는 무엇인가요?
   - a) 반복문을 처리하기 위해
   - b) 데이터를 삽입하기 위해
   - c) 객체간의 관계를 표현하기 위해
   - d) 데이터를 삭제하기 위해
   
   정답: c) 객체간의 관계를 표현하기 위해
   이유: <association> 요소는 resultMap 내에서 복합 객체를 표현하기 위해 사용됩니다. 즉, 객체 간의 관계를 표현하는데 사용됩니다.

6. 동적 쿼리에서 조건문을 처리하기 위한 태그는 무엇인가요?
   - a) <choose>
   - b) <if>
   - c) <foreach>
   - d) <where>
   
   정답: b) <if>
   이유: MyBatis에서는 <if> 태그를 사용하여 조건문을 처리할 수 있습니다. 이는 SQL 쿼리 내에서 조건에 따라 다른 SQL을 생성하는 데 사용됩니다.

7. 스프링에서 파일 업로드 진행 프로세스 중, 등록된 파일이 특정 위치에 저장되고, DB에도 저장되는 단계는 무엇인가요?
   - a) 화면이 로딩되면 첨부 파일을 하는 항목이 있고
   - b) 해당 항목을 클릭을 통해 파일을 첨부한다.
   - c) 등록버튼을 통해서 해당 파일을 서버에 등록 처리한다.
   - d) 등록된 파일을 특정 위치에 저장되고, DB에도 저장이 된다.
   
   정답: d) 등록된 파일을 특정 위치에 저장되고, DB에도 저장이 된다.
   이유: 스프링에서 파일 업로드는 사용자가 파일을 선택하고 등록 버튼을 누르면 해당 파일이 서버의 특정 위치에 저장되고, 파일 정보가 데이터베이스에 저장되는 프로세스를 거칩니다.

8. Mybatis에서 <foreach> 태그의 'open' 속성이 하는 역할은 무엇인가요?
   - a) 반복형식의 시작을 표현한다.
   - b) 반복형식의 구분자를 표현한다.
   - c) 반복형식의 종료를 표현한다.
   - d) 반복형식의 단위데이터를 표현한다.
   
   정답: a) 반복형식의 시작을 표현한다.
   이유: MyBatis의 <foreach> 태그에서 'open' 속성은 반복문이 시작될 때 삽입될 문자열을 지정합니다.

9. 동적 SQL을 만들어야 하는 주된 이유는 무엇인가요?
   - a) 데이터 처리의 효율성을 높이기 위해
   - b) 데이터의 변화에 따라 SQL의 변화가 필요한 경우
   - c) SQL의 구문 자체가 변경되는 것을 방지하기 위해
   - d) 모든 상황에 대해 동일한 SQL을 사용하기 위해
   
   정답: b) 데이터의 변화에 따라 SQL의 변화가 필요한 경우
   이유: 동적 SQL은 데이터의 상태나 조건에 따라 다양한 SQL을 생성할 필요가

 있을 때 사용됩니다. 즉, 데이터의 변화에 따라 SQL의 변화가 필요한 경우에 주로 사용됩니다.

10. MyBatis에서 객체간의 관계를 표현하기 위해 어떤 요소를 사용하나요?
   - a) <choose>
   - b) <association>
   - c) <if>
   - d) <foreach>
   
   정답: b) <association>
   이유: MyBatis에서 객체 간의 관계를 표현하거나 결합할 때는 <association> 요소를 사용합니다. 이는 결과 맵 내에서 객체 간의 복잡한 관계를 표현하는 데 사용됩니다.
   
   