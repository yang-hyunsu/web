1. 다운로드 처리를 위해 서버에서 클라이언트로 파일을 전송하는 방식은 무엇인가요?
   1) HTTP Response를 통해 파일을 전송하는 방식
   2) FTP를 사용하여 파일을 전송하는 방식
   3) 웹소켓을 활용하여 파일을 전송하는 방식
   4) JSON 형식으로 파일 데이터를 전송하는 방식

2. 스프링에서 파일 다운로드 처리를 위해 사용하는 뷰 리졸버의 역할은 무엇인가요?
   1) 파일 업로드를 처리하는 뷰를 등록하는 역할
   2) 클라이언트에게 전송할 파일의 경로와 정보를 포함시키는 역할
   3) 파일 다운로드를 위한 뷰 클래스를 컨테이너에 등록하는 역할
   4) 파일 다운로드를 처리하는 뷰 객체를 생성하는 역할

3. 다운로드 처리를 위해 뷰 클래스를 선언하는 방법에 대해서 설명한 것은 무엇인가요?
   1) 스프링 설정 파일에 `<bean>` 태그를 사용하여 뷰 클래스를 등록하는 방법
   2) `@Component` 어노테이션을 사용하여 뷰 클래스를 등록하는 방법
   3) `@Configuration` 어노테이션이 선언된 클래스에서 `WebMvcConfigurer`를 구현하여 뷰 클래스를 등록하는 방법
   4) `@Controller` 어노테이션이 선언된 클래스에서 뷰 클래스를 등록하는 방법

4. 다운로드 처리를 위해 뷰 클래스를 컨테이너에 등록하는 설정 코드는 어떤 것인가요?
   1) `<bean id="downloadViewer" class="springweb.a06_util.DownloadViewer"/>`
   2) `<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver"/>`
   3) `<bean id="jsonView" class="org.springframework.web.servlet.view.json.MappingJackson2JsonView"/>`
   4) `<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver"/>`

5. 파일 다운로드를 처리하는 뷰 클래스에서 클라이언트로 전송할 파일을 가져오기 위해 사용하는 객체는 무엇인가요?
   1) `HttpServletResponse`
   2) `HttpServletRequest`
   3) `File`
   4) `ResponseEntity`

6. 파일 다운로드를 처리하는 뷰 클래스에서 파일명을 한글로 인코딩하는 방법은 무엇인가요?
   1) `response.setContentType("application/octet-stream; charset=UTF-8")`을 설정하는 방법
   2) `response.setHeader("Content-Disposition", "attachment; filename=\"" + URLEncoder.encode(filename, "UTF-8") + "\"")`을 설정하는 방법
   3) `response.setCharacterEncoding("UTF-8")`을 설정하는 방법
   4) `response.setContentType("text/html; charset=UTF-8")`을 설정하는 방법

8. 파일 다운로드를 처리하는 뷰 클래스에서 파일의 크기와 파일명을 HTTP 응답 헤더에 포함시키는 방법은 무엇인가요?
    1) `response.setContentLength(file.length())`과 `response.setHeader("Content-Disposition", "attachment; filename=\"" + filename + "\"")`을 설정하는 방법
    2) `response.setContentLength(file.length())`과 `response.setHeader("Content-Length", file.length())`을 설정하는 방법
    3) `response.setContentLengthLong(file.length())`과 `response.setHeader("Content-Disposition", "attachment; filename=\"" + filename + "\"")`을 설정하는 방법
    4) `response.setContentLengthLong(file.length())`과 `response.setHeader("Content-Length", file.length())`을 설정하는 방법

9. 파일 다운로드를 처리하는 뷰 클래스에서 파일을 서버에서 가져오기 위해 사용하는 클래스는 무엇인가요?
    1) `FileInputStream`
    2) `FileReader`
    3) `BufferedInputStream`
    4) `ObjectInputStream`

10. 파일 다운로드를 처리하는 뷰 클래스에서 파일을 클라이언트로 전송하기 위해 사용하는 객체는 무엇인가요?
    1) `OutputStream`
    2) `InputStream`
    3) `DataOutputStream`
    4) `PrintStream`

# 정답

1. 다운로드 처리를 위해 서버에서 클라이언트로 파일을 전송하는 방식은 무엇인가요?
   1) HTTP Response를 통해 파일을 전송하는 방식
   2) FTP를 사용하여 파일을 전송하는 방식
   3) 웹소켓을 활용하여 파일을 전송하는 방식
   4) JSON 형식으로 파일 데이터를 전송하는 방식
   정답: 1

2. 스프링에서 파일 다운로드 처리를 위해 사용하는 뷰 리졸버의 역할은 무엇인가요?
   1) 파일 업로드를 처리하는 뷰를 등록하는 역할
   2) 클라이언트에게 전송할 파일의 경로와 정보를 포함시키는 역할
   3) 파일 다운로드를 위한 뷰 클래스를 컨테이너에 등록하는 역할
   4) 파일 다운로드를 처리하는 뷰 객체를 생성하는 역할
   정답: 3

3. 다운로드 처리를 위해 뷰 클래스를 선언하는 방법에 대해서 설명한 것은 무엇인가요?
   1) 스프링 설정 파일에 `<bean>` 태그를 사용하여 뷰 클래스를 등록하는 방법
   2) `@Component` 어노테이션을 사용하여 뷰 클래스를 등록하는 방법
   3) `@Configuration` 어노테이션이 선언된 클래스에서 `WebMvcConfigurer`를 구현하여 뷰 클래스를 등록하는 방법
   4) `@Controller` 어노테이션이 선언된 클래스에서 뷰 클래스를 등록하는 방법
   정답: 2

4. 다운로드 처리를 위해 뷰 클래스를 컨테이너에 등록하는 설정 코드는 어떤 것인가요?
   1) `<bean id="downloadViewer" class="springweb.a06_util.DownloadViewer"/>`
   2) `<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver"/>`
   3) `<bean id="jsonView" class="org.springframework.web.servlet.view.json.MappingJackson2JsonView"/>`
   4) `<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver"/>`
   정답: 1

5. 파일 다운로드를 처리하는 뷰 클래스에서 클라이언트로 전송할 파일을 가져오기 위해 사용하는 객체는 무엇인가요?
   1) `HttpServletResponse`
   2) `HttpServletRequest`
   3) `File`
   4) `ResponseEntity`
   정답: 1

6. 다운로드 처리를 위해 컨트롤러에서 사용하는 `ResponseEntity`의 역할은 무엇인가요?
   1) HTTP 응답을 생성하여 클라이언트로 전송하는 역할
   2) 파일 다운로드를 처리하는 뷰 클래스를 생성하는 역할
   3) 서버에서 클라이언트로 데이터를 전송하는 역할
   4) 클라이언트의 요청에 대한 응답을 생성하는 역할
   정답: 1

7. 파일 다운로드를 처리하는 뷰 클래스에서 파일명을 한글로 인코딩하는 방법은 무엇인가요?
    1) `response.setContentType("application/octet-stream; charset=UTF-8")`을 설정하는 방법
    2) `response.setHeader("Content-Disposition", "attachment; filename=\"" + URLEncoder.encode(filename, "UTF-8") + "\"")`을 설정하는 방법
    3) `response.setCharacterEncoding("UTF-8")`을 설정하는 방법
    4) `response.setContentType("text/html; charset=UTF-8")`을 설정하는 방법
    정답: 2

8. 파일 다운로드를 처리하는 뷰 클래스에서 파일의 크기와 파일명을 HTTP 응답 헤더에 포함시키는 방법은 무엇인가요?
    1) `response.setContentLength(file.length())`과 `response.setHeader("Content-Disposition", "attachment; filename=\"" + filename + "\"")`을 설정하는 방법
    2) `response.setContentLength(file.length())`과 `response.setHeader("Content-Length", file.length())`을 설정하는 방법
    3) `response.setContentLengthLong(file.length())`과 `response.setHeader("Content-Disposition", "attachment; filename=\"" + filename + "\"")`을 설정하는 방법
    4) `response.setContentLengthLong(file.length())`과 `response.setHeader("Content-Length", file.length())`을 설정하는 방법
    정답: 1

9. 파일 다운로드를 처리하는 뷰 클래스에서 파일을 서버에서 가져오기 위해 사용하는 클래스는 무엇인가요?
    1) `FileInputStream`
    2) `FileReader`
    3) `BufferedInputStream`
    4) `ObjectInputStream`
    정답: 1

10. 파일 다운로드를 처리하는 뷰 클래스에서 파일을 클라이언트로 전송하기 위해 사용하는 객체는 무엇인가요?
    1) `OutputStream`
    2) `InputStream`
    3) `DataOutputStream`
    4) `PrintStream`
    정답: 1

